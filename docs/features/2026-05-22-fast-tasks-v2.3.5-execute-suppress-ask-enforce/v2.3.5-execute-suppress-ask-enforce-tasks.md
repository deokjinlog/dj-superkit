# v2.3.5 — execute-plan 중 non-critical 재질문 지양 + AskUserQuestion 도구 강제 (fast-tasks, 5개)

**Mode**: fast-tasks (Mini Socratic skip — 백로그 358 lines 매우 명확 + 사용자 "다 진행")
**Backlog**: `docs/backlog/v2.3.5-execute-plan-suppress-noncritical-reprompt.md`

---

## Task 1: `skills/executing-plans/SKILL.md` 끝에 A 룰 1~4 + B 룰 1~4 + Anti-Patterns boilerplate 추가

- **명세**: line 286 "Related Skills" 뒤에 신규 섹션 append:
  - `## Critical / Non-critical 판정 룰 (v2.3.5+)` — 룰 1 (critical 7 케이스 표) + 룰 2 (non-critical 6 케이스 표) + 룰 3 (모드 위임 의미) + 룰 4 (BLOCKED self-correct → 사용자 재질문)
  - `## 사용자 질문 = AskUserQuestion 도구 (v2.3.5+)` — 룰 B-1~4 boilerplate (전역 룰 cross-reference + skill body 안 강제)
  - `## Anti-Patterns (v2.3.5)` — A 8 라인 + B 8 라인
- **영향 파일**: `skills/executing-plans/SKILL.md`

## Task 2: `skills/js-super-sub-driven/SKILL.md` 끝에 같은 boilerplate mirror

- **명세**: line 446 "Related Skills" 뒤에 T1 과 동일 boilerplate. subagent mode 특화 표현:
  - 룰 4 의 "self-correct" 는 W-2 분기 (implementer BLOCKED → 메인 reorder dispatch 자동) 와 정합 명시
  - 룰 2 의 "wave 단위 보고" 는 v2.0.0+ Per-wave Sequence 그대로 (변경 X)
  - B 룰은 동일 mirror
- **영향 파일**: `skills/js-super-sub-driven/SKILL.md`

## Task 3: `CLAUDE.md` 끝에 글로벌 룰 + 3 파일 결합 메모 추가

- **명세**: line 454 끝에 두 섹션 신규 append:
  - `## AskUserQuestion 도구 우선 (v2.3.5+)` — B 글로벌 룰 (skill body 외 ad-hoc 결정에도 적용) + 룰 B-2 예외 3 케이스 (자유 텍스트 요청 / 단순 ack / 상태 보고) + 알람 fire 보장
  - `## execute-plan critical/non-critical + AskUserQuestion 강제 결합 (v2.3.5+)` — 3 파일 atomic patch 룰 + 회귀 catch grep + 보존 룰 (v1.1.12 자동승인 / v2.0.0 byte-copy reorder / v2.0.1 same-file 묶음 / v2.0.3 8 skill boilerplate / v2.1.1 Other 룰) 그대로
- **영향 파일**: `CLAUDE.md`

## Task 4: 6 manifest version bump 2.3.4 → 2.3.5

- **명세**: mechanical version bump
- **영향 파일**: `gemini-extension.json`, `package.json`, `.cursor-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`

## Task 5: 백로그 `shipped/` 이관

- **명세**: `git mv docs/backlog/v2.3.5-execute-plan-suppress-noncritical-reprompt.md docs/backlog/shipped/`
- **영향 파일**: `docs/backlog/v2.3.5-execute-plan-suppress-noncritical-reprompt.md` → `docs/backlog/shipped/`

---

## 병렬화 계획 (DAG)

- **Independent**: 1, 2, 3, 4, 5 — 모두 다른 파일 / 다른 디렉토리. 의존성 0.
- 메인 직접 처리 (v2.3.0~v2.3.4 답습 — fast-tasks 의 "smart parallel dispatch" 룰을 메인 직접으로 변형. v2.3.5 룰 본문이 길고 정확성 요구라 subagent dispatch 대신 메인이 직접 적용 + per-task commit).

## 회귀 catch (백로그에서 발췌)

```bash
# A 룰 박힘 확인
grep -c "Critical / Non-critical 판정 룰 (v2.3.5+)" \
  skills/executing-plans/SKILL.md skills/js-super-sub-driven/SKILL.md
# expected: 각 1

# B 룰 박힘 확인
grep -c "사용자 질문 = AskUserQuestion 도구 (v2.3.5+)" \
  skills/executing-plans/SKILL.md skills/js-super-sub-driven/SKILL.md
# expected: 각 1

# CLAUDE.md 글로벌 룰
grep -c "AskUserQuestion 도구 우선 (v2.3.5+)" CLAUDE.md
# expected: ≥ 1

# 안티 패턴 catch (잘못된 게이트 표현이 본문 정상 부분에 들어가지 않았는지)
grep -nE "병렬.*해도.*될까|묶을까|다음 task.*진입할까" \
  skills/executing-plans/SKILL.md skills/js-super-sub-driven/SKILL.md
# expected: 0 (Anti-Pattern catch 라인만 허용)

grep -nE "이렇게.*할까요\?|어느.*쪽.*인가요\?" \
  skills/executing-plans/SKILL.md skills/js-super-sub-driven/SKILL.md
# expected: 0 (Anti-Pattern catch 라인만 허용)
```

## 보존 (영향 0 보장)

- v1.1.12+ 자동승인 X 룰 — A 룰 1 7 케이스 안에서 유효
- v2.0.0+ byte-copy + reorder 3-stage — A 룰 4 의 자가 복구 핵심 (변경 X)
- v2.0.1+ same-file mechanical 묶음 — A 룰 2 의 자율 묶음 근거 (변경 X)
- v2.0.3+ 8 skill AskUserQuestion 강제 boilerplate — B 룰의 skill body 안 일관성 (변경 X)
- v2.1.1+ Other / 모호 응답 처리 — B 룰의 재질문 패턴 결합 (변경 X)
- writing-plans `**Model**:` 필드 — A 룰 2 의 dispatch model 결정 근거 (변경 X)
- 알람 시스템 `repeat-alert.sh` 4-layer — fire 조건 정상화 (변경 X)
- og-* / auto-* 영향 0 — auto-* 는 Socratic only 룰 (B 명시 예외)
- `scripts/preflight.py` / `scripts/plan_byte_check.py` 영향 0 — 실행 단계 룰만

## 생략 (fast-tasks 룰 — 영구 생략)

- `<slug>-requirements.md` / `<slug>-tech-design.md` / `<slug>-implementation-plan.md` (3-MD 전체)
- `verifying-spec` 4축 보고서
- 변경이력 footer / `change-history` skill 호출
- `risk-annotation` (RISK 주석)
- `generating-html` / `code-pretty` 호출
- AskUserQuestion 게이트 (본 batch 자체는 fast-tasks — 게이트 없음. 단 Task 1/2/3 에서 추가되는 룰 자체는 AskUserQuestion 도구 사용 강제)
