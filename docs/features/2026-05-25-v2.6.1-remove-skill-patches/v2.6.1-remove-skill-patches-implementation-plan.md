---
slug: v2.6.1-remove-skill-patches
commit_policy: per-task
execution_mode: main-inline
note: "메인 inline 실행 가정 — `**원본**` 블록 생략. patch + 신규 1 파일 + manifest version bump 작업이라 byte-copy 의미 낮음."
---

# v2.6.1 — `/remove-skill` 신규 + critical 3건 patch (implementation-plan)

**Slug**: v2.6.1-remove-skill-patches
**작성일**: 2026-05-25
**총 task 수**: 4 (Wave 0~3 + spec/[log])
**참조**: requirements (CH-20260525-001), tech-design (CH-20260525-002)

---

## §1. Tasks

### Wave 0 — CLAUDE.md 결합 메모 patch + 신규 섹션

#### Task 1 — CLAUDE.md 의 v2.6.0+ 섹션에 v2.6.1+ 분기 + 신규 `/remove-skill` 섹션 추가

- **Files**: `CLAUDE.md`
- **Model**: sonnet (Korean prose + 결합 메모 + 기존 v2.6.0+ 양식 일관)
- **TDD steps**:
  1. 기존 `## /new-skill 빌더 결합 (v2.6.0+)` 섹션 안에 v2.6.1+ 분기 sub-section 추가 — patch 3건 명시 (description 좁히기 / step 폭주 alert / `--force` 백업 3 step)
  2. 파일 끝에 `## /remove-skill 빌더 결합 (v2.6.1+)` 섹션 신규 추가
     - 적용 범위 (1 파일 — `commands/remove-skill.md`)
     - 핵심 룰 (safe-rename default / `--force` opt-in hard-delete / `--dry-run` preview / `/reload-plugins` 안내)
     - 회귀 패턴 (한쪽만 변경 시)
     - 회귀 catch grep (`-F` fixed string)
     - 영향 범위
  3. 회귀 grep: `grep -F "## /remove-skill 빌더 결합 (v2.6.1+)" CLAUDE.md` ≥ 1 + `grep -F "v2.6.1+ 분기" CLAUDE.md` ≥ 1
- **RISK**: side-effect (contributor 메모 갱신)
- **Self-Review**: v2.0.4+ / v2.5.1+ 분기 sub-section 패턴 답습. R-1 (rename 후 자동 발동 차단 X 명확 X) / R-2 (`--force` 데이터 손실) 회귀 catch grep 명시

### Wave 1 — commands/new-skill.md 3 patch

#### Task 2 — `commands/new-skill.md` 3 patch (same-file mechanical 묶음)

- **Files**: `commands/new-skill.md` (3 step 모두 같은 파일)
- **Model**: sonnet (Korean prose patch — Haiku rephrasing 위험)
- **묶음 검증**: D1 3 조건 모두 만족 — 같은 파일 / 테스트 경계 X (한 파일 grep 검증) / mechanical (본문 문구 정정만, 알고리즘 변경 X). → 1 task multi-step.
- **TDD steps**:
  1. **Patch A — description 좁히기**:
     - 기존: `"자유 텍스트 한 줄을 받아 글로벌 ~/.claude/skills/<slug>/SKILL.md 1 파일로 자동 생성하는 skill 빌더. ..."`
     - 변경 후: `"사용자가 /new-skill 슬래시를 명시 호출했을 때만 발동. 자유 텍스트 한 줄을 받아 글로벌 ~/.claude/skills/<slug>/SKILL.md 1 파일로 자동 생성."`
     - frontmatter description 필드만 edit
  2. **Patch B — step 폭주 alert 정정** (§ 3-2):
     - 기존: `"그대로 진행하시려면 `--force` 옵션 추가하거나 다시 호출해주세요."`
     - 변경 후: `"그대로 진행하려면 같은 명령을 다시 호출해주세요. 분리하려면 자유 텍스트를 두 개로 나눠 두 번 호출해주세요."`
     - § 3-2 본문 alert 메시지 edit
  3. **Patch C — `--force` 백업 instruction 명시화** (§ 5-3):
     - 기존: `"1. 기존 파일을 ~/.claude/skills/<slug>/SKILL.md.bak-<YYYYMMDDHHMMSS> 로 백업 (Write 도구로 copy 하거나 별도 안내)"`
     - 변경 후 — 3 step 명시:
       ```
       1. Read 도구로 기존 ~/.claude/skills/<slug>/SKILL.md 읽기
       2. Write 도구로 ~/.claude/skills/<slug>/SKILL.md.bak-<YYYYMMDDHHMMSS> 에 같은 내용 저장
       3. Write 도구로 ~/.claude/skills/<slug>/SKILL.md 덮어쓰기
       ```
  4. **본문 전체 grep 회귀 검증**: `grep -cF "--force" commands/new-skill.md` 결과가 합리적 범위 (≈ 6~10, 옵트인 옵션 안내 + 백업 분기 본문 등). step 폭주 alert 의 `--force` 잘못 인용 제거 확인.
  5. **회귀 grep**:
     - `grep -F "명시 호출" commands/new-skill.md` ≥ 1 (Patch A)
     - `grep -F "분리하려면 자유 텍스트를" commands/new-skill.md` ≥ 1 (Patch B)
     - `grep -F "Read 도구로" commands/new-skill.md` ≥ 1 (Patch C)
- **RISK**: side-effect (description 좁히기 — R-3) + side-effect (alert 정정 — R-4) + race (백업 3 step — R-5)
- **Self-Review**:
  - 본문 전체 grep `--force` 의 의미 확인 — 옵트인 옵션 / 백업 분기 / 보고 양식 외에 다른 잘못된 인용 없음
  - Patch A description 길이 max 120자 룰 (D-T3 자체 룰) 검증 — 새 description 약 100자
  - Patch C 3 step 의 atomic 보장 (단일 turn 흐름)

### Wave 2 — commands/remove-skill.md 신규 작성

#### Task 3 — `commands/remove-skill.md` 신규 (instruction-only 빌더 본문)

- **Files**: `commands/remove-skill.md`
- **Model**: sonnet (LLM 분해 룰 / 보고 양식 / 한국어 친화 톤 / `/new-skill` 답습 패턴)
- **TDD steps**:
  1. frontmatter 작성:
     ```yaml
     ---
     description: "사용자가 /remove-skill 슬래시를 명시 호출했을 때만 발동. 글로벌 ~/.claude/skills/<slug>/ 디렉토리를 안전하게 정리 (default = .removed-<timestamp> 로 rename, --force = rm -rf hard-delete)."
     argument-hint: "<slug> [--force] [--dry-run]"
     ---
     ```
  2. 본문 6 섹션 작성 (`/new-skill` 답습 + remove 흐름 맞춤):
     - `## 1. 사전 검증` — `$ARGUMENTS` 비어있음 catch + 사용 예시 안내 + slug 인자 필수 (`/new-skill` 과 달리 slug 필수)
     - `## 2. 입력 파싱` — slug 인자 (첫 토큰) + 플래그 (`--force` / `--dry-run`) 분리
     - `## 3. 존재 검증 + 메타데이터 수집` — LS 도구로 `~/.claude/skills/<slug>/` 존재 확인 + 크기 + 마지막 수정 timestamp
     - `## 4. 분기 실행`:
       - 부재 시 abort + 현재 글로벌 skill 목록 안내 (R-7)
       - `--dry-run` → preview 메인 응답
       - default → Bash 도구로 `mv ~/.claude/skills/<slug> ~/.claude/skills/<slug>.removed-<YYYYMMDDHHMMSS>` (D-T1 safe-rename)
       - `--force` → Bash 도구로 `rm -rf ~/.claude/skills/<slug>` (D-T2 hard-delete, 옵트인 명시)
     - `## 5. 보고 양식` — D-T1 보고 양식 4 패턴 (safe-rename / hard-delete / dry-run / 부재)
     - `## 6. 금지` — slug 인자 누락 시 abort / `--force` 자동 적용 X / 다른 위치 (`<plugin>/skills/`) 검증 X
  3. 회귀 grep:
     - `test -f commands/remove-skill.md` PASS
     - `grep -F "## 4. 분기 실행" commands/remove-skill.md` ≥ 1
     - `grep -F "safe-rename" commands/remove-skill.md` ≥ 1
- **RISK**: side-effect (신규 파일, R-1 / R-7 / R-8 본문에 룰 박힘) + breaking (`--force` hard-delete, R-2) + shared state (rename 후 자동 발동 차단 보장 X, R-1)
- **Self-Review**:
  - `/new-skill` 답습 — instruction-only, bash 호출은 `## 4. 분기 실행` 의 mv / rm -rf 한 줄만 (D-T2 명시)
  - 본문 한국어 친화 톤 (v2.4+ A-1~A-5)
  - 보고 메시지에 `/reload-plugins` 안내 (R-1 완화)
  - timestamp `YYYYMMDDHHMMSS` 형식 통일 (R-8)
  - 부재 시 현재 skill 목록 안내 (R-7)

### Wave 3 — 6 manifest 버전 bump

#### Task 4 — 6 manifest 통일 2.6.0 → 2.6.1

- **Files**: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.cursor-plugin/plugin.json`, `.codex-plugin/plugin.json`, `gemini-extension.json`, `package.json`
- **Model**: haiku (mechanical version string swap)
- **TDD steps**:
  1. 각 manifest 의 `"version"` 필드 swap (`"version": "2.6.0"` → `"version": "2.6.1"`)
  2. 회귀 grep: `grep -F '"version": "2.6.0"'` 0 매치 + `grep -F '"version": "2.6.1"'` 6 매치
- **RISK**: side-effect (R-6 누락 catch grep)
- **Self-Review**: 6 파일 모두 갱신

---

## §2. 위험 코드 지점

### R-1 (rename 후 자동 발동 차단 보장 명확 X)

- **위치**: `commands/remove-skill.md` `## 4. 분기 실행` + `## 5. 보고 양식` (Task 3) + `CLAUDE.md` 결합 메모 (Task 1)
- **변경**: 보고 메시지에 `/reload-plugins` 호출 안내 추가 + 결합 메모에 "rename 만으로 차단 보장 X" 명시
- **Mitigation**: 사용자 catch 영역. v2.6.x dogfood 검증으로 추가 patch 여지 (v2.6.2 후보).

### R-2 (`--force` hard-delete 데이터 손실)

- **위치**: `commands/remove-skill.md` `## 4. 분기 실행` (Task 3) + 보고 양식 (Task 3)
- **변경**: 옵트인 플래그 명시 + abort 메시지에 "회복 불가" 강조 + 다시 만들려면 `/new-skill` 안내
- **Mitigation**: 명시 플래그만 + 안전 default (D-T1 safe-rename).

### R-3 (description 좁힌 후에도 자동 발동 가능)

- **위치**: `commands/new-skill.md` frontmatter description (Task 2 Patch A)
- **변경**: "명시 호출 only" 톤 우선
- **Mitigation**: dogfood 검증 영역. 100% 보장 X — v2.6.x 후속 patch 여지.

### R-4 (alert 정정 본문 다른 곳 누락)

- **위치**: `commands/new-skill.md` § 3-2 (Task 2 Patch B)
- **변경**: § 3-2 정정 + 본문 전체 `--force` grep 으로 다른 잘못된 인용 없음 검증
- **Mitigation**: Task 2 step 4 의 grep 검증 + Self-Review.

### R-5 (`--force` 백업 3 step atomic X)

- **위치**: `commands/new-skill.md` § 5-3 (Task 2 Patch C)
- **변경**: 3 step 명시 (Read → Write bak → Write 덮어쓰기)
- **Mitigation**: 단일 turn 흐름 — 사용자 ctrl+c 케이스만 우려, 사용자 catch 영역.

### R-6 (6 manifest 누락 회귀)

- **위치**: 6 manifest (Task 4)
- **변경**: 6 파일 모두 swap
- **Mitigation**: grep `"2.6.0"` 0 매치 + `"2.6.1"` 6 매치 검증.

### R-7 (부재 skill abort 부실)

- **위치**: `commands/remove-skill.md` `## 4. 분기 실행` 부재 분기 + 보고 양식 (Task 3)
- **변경**: 부재 시 현재 글로벌 skill 목록 (LS `~/.claude/skills/`) 함께 안내
- **Mitigation**: 보고 양식 명시.

### R-8 (`.removed-` 접미사 충돌)

- **위치**: `commands/remove-skill.md` `## 4. 분기 실행` rename 명령 (Task 3)
- **변경**: timestamp `YYYYMMDDHHMMSS` 단위 unique 보장
- **Mitigation**: 동시 호출 0 가정 (단일 사용자).

---

## §3. 변경이력

### [2026-05-25 23:25] [구현계획서-수정]
- **id**: CH-20260525-003
- **이유**: v2.6.1 patch 최초 implementation-plan 작성 (Wave 0~3, 4 task)
- **무엇이**: v2.6.1-remove-skill-patches-implementation-plan.md 전체 (frontmatter + §1 Task 1~4 + §2 R-1~R-8 위험 매핑)
- **영향범위**: tech-design D-T1~D-T7 + R-1~R-8 모두 task / 위험 코드 지점 매핑. Task 2 의 3 patch 가 same-file mechanical 묶음 룰 (D1 3 조건) 만족하여 1 task multi-step 묶음. plan_byte_check PASS (0 mismatches — slim plan). verifying-spec PASS (gaps 0 / conflicts 0).
- **연관 항목**: CH-20260525-001 (requirements), CH-20260525-002 (tech-design)
