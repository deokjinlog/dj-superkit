# v2.4.2 — 설명 방식 자체를 자연스럽게 (tech-design)

**Slug**: `v2.4.2-commands-md-tone-polish`
**연관**: `v2.4.2-commands-md-tone-polish-requirements.md` (CH-20260523-001)
**Mode**: auto-design (adaptive 7-topic — 활성 5 / 비활성 2)

---

## §1 시스템 컨텍스트 / 영향 매트릭스

본 patch 는 markdown 본문 정리만 합니다. skill body 의 룰 / 로직 / 코드 호출 흐름은 그대로 두고, 사용자 환경에 실제 전달되는 영역의 한글 본문만 자연스러운 줄글로 풀어쓰는 작업이에요.

### 1.1 변경 대상 (광범위 23 파일 + CLAUDE.md 메타 노트 1 섹션)

| 컴포넌트 | 변경 종류 | Wave |
|---|---|---|
| `CLAUDE.md` | contributor 안전망 메타 노트 1 섹션 추가 (적용 범위 안내) | Wave 0 (이미 적용됨) |
| `skills/brainstorming/SKILL.md` | 사용자 노출 본문 광범위 정리 | Wave 1 |
| `skills/designing-direction/SKILL.md` | 동 | Wave 1 |
| `skills/writing-plans/SKILL.md` | 동 | Wave 1 |
| `skills/executing-plans/SKILL.md` | 동 | Wave 1 |
| `skills/auto-brainstorming/SKILL.md` | 동 | Wave 2 |
| `skills/auto-designing-direction/SKILL.md` | 동 | Wave 2 |
| `skills/auto-writing-plans/SKILL.md` | 동 | Wave 2 |
| `skills/auto-executing-plans/SKILL.md` | 동 | Wave 2 |
| `commands/*.md` (15 파일) | 사용자 안내문 광범위 정리 | Wave 3 |
| 6 manifest | 2.4.1 → 2.4.2 | Wave 4 |
| `docs/features/2026-05-23-v2.4.2-commands-md-tone-polish/` | shipped 이관 (백로그 없음) | Wave 4 |

og-* 3 skill / 3 command, audit-report-prompt, change-history / risk-annotation / verifying-spec / generating-html / code-pretty 등 보조 skill 은 영향 0. v2.4.x 룰 / 알람 시스템 / scripts 영향 0.

### 1.2 변경 정신 — 룰 추가 아닌 본문 정리

v2.4.0 의 A 항목이 CLAUDE.md 에 룰 박는 방식이었던 반면, 본 v2.4.2 는 룰 추가 X 입니다. 사용자 환경에 실제 전달되는 23 파일 본문을 직접 자연스럽게 풀어쓰는 작업이에요. CLAUDE.md 의 v2.4 A 룰은 자기 참조용으로 그대로 두고, 위에서 박은 contributor 안전망 메타 노트만 예외로 추가.

## §2 영향 컴포넌트 (자세히)

### 2.1 정리 영역 5종

각 skill body 안에서 다음 5 영역을 사용자 노출로 간주하고 풀어씁니다.

- **ℹ️ 안내 라인** — v2.4 Wave 2/3 의 직접 연장. 더 광범위하게.
- **Process / Step 본문 prose** — `Step N — ...` 안의 한글 설명. 표 / 형식 헤더 / 메뉴식 옵션 다발 줄임.
- **Anti-Patterns 표 안 한글 본문** — `Wrong | Right` 표의 한글 셀만. 영어 식별자 셀은 그대로.
- **Why / How to apply 안 한글 본문** — 룰 박스 안 한글 prose.
- **사용자 인용 / 회귀 catch 메모** — `> 사용자 인용` 같은 줄.

영어 식별자 (`AskUserQuestion`, `TaskCreate`, `plan_byte_check`, `generating-html`, `auto-brainstorming` 등) 와 함수명 / 파일 경로 / 도구명, git 표준 용어 (`commit`, `push`, `merge`, `tag`) 는 그대로 유지.

### 2.2 commands/*.md 정리 영역

각 슬래시 명령 파일 안의 모든 사용자 노출 영역. description / 사용법 / 옵션 설명 / Process / Anti-Patterns 표 한글 본문 / 예시 안내 등.

og-* 3 파일 (`og-brainstorm.md` / `og-execute-plan.md` / `og-write-plan.md`) 은 upstream mirror 라 변경 0. `audit-report-prompt.md` 는 subagent prompt 본문이라 사용자 노출 X 라 제외.

### 2.3 단어 매핑 (v2.4 A-3 답습 + 확장)

v2.4 에서 박은 8 매핑을 답습하면서, 본 작업 중 catch 한 추가 영어 단어는 즉시 매핑 박고 정리. 현재 답습 매핑:

`dispatch` → `호출`, `fire-and-forget` → `백그라운드 호출`, `byte-copy` → `원본 그대로 보존`, `wave-parallel` → `여러 작업 동시 진행`, `boilerplate` → `공통 문구`, `manifest` → `설정 파일` / `manifest`, `slug` → `폴더명` / `slug`, `override` → `자동 통과` / `덮어쓰기`.

추가 catch 후보 (작업 중 발견 시 매핑 박을 단어들):

`subagent` → `보조 에이전트`, `Gate #N` → `승인 게이트`, `Anti-Pattern` → `안티 패턴` / `금지 패턴`, `catch` → `잡다` / `알아채다` / `발견하다`, `dogfood` → `직접 써보기` / `자체 검증`, `carry-over` → `다음으로 미룬 항목`.

## §3 데이터 모델 — N/A

본 patch 는 markdown 본문 정리만이라 schema / DB / 데이터 구조 변경 X.

## §4 외부 인터페이스 — N/A

API / DB / 외부 서비스 호출 변경 X. 플러그인 cache / marketplace 경로 변경 X.

## §5 핵심 결정 + 대안 비교

### D-T1: 단어 매핑만 vs 본문 자체 풀어쓰기 vs 둘 다

| 대안 | 설명 | 트레이드오프 |
|---|---|---|
| 단어 매핑만 (v2.4 A-3 답습) | 영어 단어 매핑 추가 + 본문 형식은 그대로 | 작업 빠름. 단 사용자 catch (설명 방식 자체) 미해결. |
| 본문 자체 풀어쓰기 | 단어는 그대로 두고 표 / 형식 헤더 / 메뉴식 옵션 다발만 정리 | 단어 catch 누락. 사용자 catch 부분 해결. |
| **둘 다 (채택)** | 단어 매핑 + 본문 형식 정리 같이 | 작업량 ↑. 사용자 catch 완전 해결. |

채택 이유: 사용자 catch 가 단어 + 형식 둘 다 직격. 한쪽만 처리 시 후속 v2.4.3 필요. 한 번에 처리.

### D-T2: 분할 단위 — 메이저 vs patch

| 대안 | 설명 | 트레이드오프 |
|---|---|---|
| 메이저 (v2.5.0) | 광범위 영향 + 메이저 의미 부여 | 사용자 release note 무게 ↑. v2.4 직후 바로 메이저 부담. |
| **patch (v2.4.2, 채택)** | v2.4 의 직접 연장이라 patch 적합 | 23 파일 영향이 patch 라기엔 큼. 단 룰 추가 X + 본문 정리만이라 patch 정당. |

채택 이유: v2.4 의 사용자 catch 직접 답습. 룰 추가 / 새 기능 X. patch 적합. 룰 추가가 필요하면 v2.5 메이저로 분리.

### D-T3: 정리 대상 범위 — 핵심 8 skill + commands 15 vs 모든 skill

| 대안 | 설명 | 트레이드오프 |
|---|---|---|
| 모든 skill body + commands | 23 파일 + 보조 skill (change-history / risk-annotation / verifying-spec / generating-html / code-pretty / setting-up-worktrees / finishing-a-development-branch / api-auto-testing) 추가 | 작업량 ↑↑. 보조 skill 은 사용자 노출 영역이 적어 ROI 낮음. |
| **핵심 8 skill + commands 15 (채택)** | 사용자가 자주 보는 흐름만 | 일부 보조 skill 의 ℹ️ 한 줄이 잔존. dogfood 시 catch 가능. |

채택 이유: 핵심 흐름 (PRD + auto-flow + commands) 만 손대도 사용자 체감 효과 충분. 보조 skill 은 v2.4.3 이후 후속 후보 (dogfood catch 시 추가).

### D-T4: 회귀 catch grep 시점 — 본 v2.4.2 vs v2.4.3

| 대안 | 설명 | 트레이드오프 |
|---|---|---|
| 본 v2.4.2 안에 catch grep 박기 | 미래 회귀 자동 catch | 작업량 ↑. 어떤 패턴을 catch 할지 본 작업 중 결정 필요. |
| **v2.4.3 reserved (채택)** | 본 v2.4.2 완료 후 dogfood 결과 보고 catch 패턴 결정 | 본 v2.4.2 작업 직후 회귀 catch 약함. 단 dogfood 사이클이 빠르면 회귀 catch 도 빠르게 박을 수 있음. |

채택 이유: 본 v2.4.2 작업 중 발견하는 패턴이 진짜 catch 후보. 미리 만든 grep 은 추측. 작업 후 catch 가 정확.

### D-T5: 검증 방식 — dogfood vs grep 검증 vs 둘 다

| 대안 | 설명 | 트레이드오프 |
|---|---|---|
| dogfood 만 (다음 외부 피처 흐름에서 catch) | 실제 사용자 체감 검증 | 시간 ↑. 첫 catch 까지 다른 작업 필요. |
| grep 검증 만 (영어 단어 / 메모 마커 grep 0) | 빠른 검증 | catch 패턴 미리 정해야. 사용자 체감 X. |
| **둘 다 (채택)** | grep 검증 으로 빠른 확인 + dogfood 로 사용자 체감 확정 | 작업량 ↑. 단 안전성 ↑↑. |

채택 이유: grep 검증은 작업 중 즉시 가능 + dogfood 는 사용자 catch 신호 대기. 둘 다 활용.

## §6 위험 (preliminary)

### R-1: 본문 정리 중 영어 식별자 실수로 변경

skill body 의 도구명 / 함수명 / 파일 경로 / 도구 enum 같은 영어 식별자는 변경 X 가 절대 룰. 실수로 한국어 매핑이 들어가면 메인이 도구 호출 못 함 (예: `AskUserQuestion` → `사용자질문도구` 매핑은 절대 금지).

**대응**: 작업 중 영어 식별자 grep 검증 (`grep -nE "AskUserQuestion|TaskCreate|plan_byte_check|generating-html|auto-brainstorming|verifying-spec|change-history" skills/*/SKILL.md commands/*.md`) — count 가 작업 전과 같아야 합치.

### R-2: 단어 매핑이 맥락에 안 맞는 경우

`override` → `자동 통과` vs `덮어쓰기` — 맥락 따라 다름. 일괄 치환 시 어색.

**대응**: 일괄 grep 치환 금지. 파일별 / 줄별 맥락 보고 매핑.

### R-3: skill body 의 LLM prompt 영역과 사용자 노출 영역 경계 모호

Process Step 본문 prose 가 사용자에게 그대로 노출되는 경우도 있고, 메인이 prompt 로 가공해서 사용자에게 다시 전달하는 경우도 있음. 둘이 섞임.

**대응**: 보수적으로 풀어쓰는 쪽으로. 형식 변경은 최소, 단어 매핑은 적극.

### R-4: og-* skill 실수 변경

upstream mirror 라 절대 변경 X. 본 작업 흐름에서 실수로 손대면 mirror 상태 깨짐.

**대응**: og-* 3 skill 은 작업 대상 list 에서 명시 제외. grep 검증 (`git diff HEAD~1 HEAD -- skills/og-*/SKILL.md` 빈 결과).

### R-5: 23 파일 동시 변경의 리뷰 부담

광범위 변경이라 한 commit 에 묶으면 리뷰 어려움.

**대응**: wave 단위 commit. Wave 1 (PRD 4 skill) / Wave 2 (auto-* 4 skill) / Wave 3 (commands 15) / Wave 4 (manifest + 이관). 각 wave 단위 검증 후 다음 wave.

### R-6: 본 작업의 메인 turn 답습

본 작업 중 메인이 답변에서 또 형식적 표 / 영어 단어 다발 / 메모 마커 쓰면 사용자가 catch. 사용자 catch 답습 강제.

**대응**: 본 작업 시작부터 메인 답변도 v2.4.2 룰 답습. 작업 중 자기 답변 catch 패턴 모니터링.

## §7 테스트 전략

### 7.1 grep 검증 (작업 중 즉시)

각 wave 끝에 다음 grep 으로 검증:

```bash
# 영어 식별자 변경 0 검증
git diff HEAD~1 HEAD -- skills/*/SKILL.md commands/*.md | grep -E "^[-+].*\b(AskUserQuestion|TaskCreate|plan_byte_check|generating-html|auto-brainstorming|verifying-spec|change-history|generating-html)\b" | grep -v "^[-+]$"
# expected: 식별자 자체의 추가/삭제 0 (같은 라인 안 단어 매핑은 OK)

# og-* 변경 0
git diff HEAD~1 HEAD -- skills/og-*/SKILL.md commands/og-*.md
# expected: 빈 결과
```

### 7.2 형식 패턴 catch (작업 후)

```bash
# 메모 마커 (→ ✅) 사용자 노출 영역에 잔존 여부
grep -nE "(^|[^\\\\])(→|✅|❌)" skills/{brainstorming,designing-direction,writing-plans,executing-plans,auto-*}/SKILL.md commands/*.md | grep -v og-

# 영어 단어 잔존 (v2.4 매핑 답습)
grep -nE "\b(dispatch|fire-and-forget|byte-copy|wave-parallel|boilerplate)\b" skills/{brainstorming,designing-direction,writing-plans,executing-plans,auto-*}/SKILL.md commands/*.md | grep -v og-
# expected: 결과는 의도된 사용 (Anti-Patterns 표의 영어 식별자 / 코드 패턴) 만
```

### 7.3 dogfood 검증

본 v2.4.2 push 후 다음 외부 피처 흐름에서 메인의 답변 / TaskCreate 명칭 / 진행 보고 / AskUserQuestion 본문 등이 v2.4.2 톤 답습하는지 사용자 catch.

### 7.4 pytest 답습

`scripts/tests/` 의 61 PASS 답습. 본 작업이 scripts 영향 X 라 회귀 가능성 거의 0 이지만 안전 위해 push 전 실행.

## 다음 단계

`auto-writing-plans` 로 자동 전환해서 구현 계획 (`v2.4.2-commands-md-tone-polish-implementation-plan.md`) 작성. 각 wave 의 task 분해 + Model hint (haiku/sonnet/opus) + 검증 grep + change-history entry 세팅.

## 변경이력

### [2026-05-23] [개발방향-수정]
- **id**: CH-20260523-002
- **이유**: v2.4.2 의 기술 설계 단계. requirements.md (CH-20260523-001) 의 D1~D6 을 §1~§7 schema 로 옮겨담고, 활성 5 토픽 (아키텍처 / 영향 컴포넌트 / 핵심 결정 / 위험 / 테스트 전략) 자세 설계 + 비활성 2 토픽 (데이터 모델 / 외부 인터페이스) N/A 처리.
- **무엇이**: 신규 `v2.4.2-commands-md-tone-polish-tech-design.md` — §1 영향 매트릭스 23 파일 + §2 정리 영역 5종 + 단어 매핑 답습 + 추가 catch 후보 6 단어 + §5 핵심 결정 D-T1~D-T5 (단어+본문 둘 다 / patch / 핵심 8 skill + commands 15 / catch grep v2.4.3 reserved / dogfood + grep 검증) + §6 위험 R-1~R-6 + §7 테스트 전략 4 항목.
- **영향범위**: 후속 산출물 (implementation-plan) 작성 기반. 코드 변경 23 파일 + CLAUDE.md 메타 노트 1 섹션 (이미 적용) + 6 manifest bump. og-* / scripts / 보조 skill / 알람 시스템 / DB / API 영향 0.
- **verifying-spec 결과**:
  - A1 consistency (requirements ↔ tech-design): ✅ D1~D6 모두 답습
  - A2 (requirements ↔ impl-plan): N/A (impl-plan 미작성)
  - C1 code impact: ⚠️ 23 파일 광범위 — Wave 분할 4 단계 (PRD 4 / auto-* 4 / commands 15 / manifest) 로 대응
  - C2 side effect: ⚠️ R-1 영어 식별자 실수 변경 위험 — grep 검증 강제, R-4 og-* 실수 변경 위험 — 명시 제외 + git diff 검증
