---
slug: v2.4.2-commands-md-tone-polish
commit_policy: per-task
total_tasks: 12
---

# v2.4.2 — 설명 방식 자체를 자연스럽게 (구현계획)

**연관**: `v2.4.2-commands-md-tone-polish-requirements.md` (CH-20260523-001) / `v2.4.2-commands-md-tone-polish-tech-design.md` (CH-20260523-002)

## §1 작업 단위 (12 task, 4 wave)

| Wave | Task | 파일 / 모드 | byte-copy |
|---|---|---|---|
| 1 | T1 — brainstorming skill 본문 한국어 친화 풀어쓰기 | brainstorming/SKILL.md (sonnet) | ❌ rewriting |
| 1 | T2 — designing-direction skill 본문 풀어쓰기 | designing-direction/SKILL.md (sonnet) | ❌ rewriting |
| 1 | T3 — writing-plans skill 본문 풀어쓰기 | writing-plans/SKILL.md (sonnet) | ❌ rewriting |
| 1 | T4 — executing-plans skill 본문 풀어쓰기 | executing-plans/SKILL.md (sonnet) | ❌ rewriting |
| 2 | T5 — auto-brainstorming skill 본문 풀어쓰기 | auto-brainstorming/SKILL.md (sonnet) | ❌ rewriting |
| 2 | T6 — auto-designing-direction skill 본문 풀어쓰기 | auto-designing-direction/SKILL.md (sonnet) | ❌ rewriting |
| 2 | T7 — auto-writing-plans skill 본문 풀어쓰기 | auto-writing-plans/SKILL.md (sonnet) | ❌ rewriting |
| 2 | T8 — auto-executing-plans skill 본문 풀어쓰기 | auto-executing-plans/SKILL.md (sonnet) | ❌ rewriting |
| 3 | T9 — auto-* 4 commands 풀어쓰기 | commands/auto-{brainstorm,design,execute-plan,write-plan}.md (sonnet) | ❌ rewriting |
| 3 | T10 — PRD 4 commands 풀어쓰기 | commands/{brainstorm,design,execute-plan,write-plan}.md (sonnet) | ❌ rewriting |
| 3 | T11 — 기타 7 commands 풀어쓰기 | commands/{api-test,audit-risk,fast-tasks,pretty-md,sync-html,worktree,worktree-merge-back}.md (sonnet) | ❌ rewriting |
| 4 | T12 — 6 manifest bump + 백로그 이관 + CLAUDE.md 메타 노트 already-applied 확인 | 6 manifest + docs/features/ (haiku) | ✅ multi-step |

### Wave 간 의존성

Wave 1~3 모두 독립 (다른 skill / 다른 command 파일). DAG dependency 없음. Wave 4 는 다른 wave 끝난 후 (commit log 답습 위해) 마지막.

같은 파일 만지는 task chain 없음 (T1~T11 각자 다른 파일). v2.0.1+ same-file 묶음 룰 자체 검토 = 묶을 거 없음.

### byte-copy 룰 면제 (v2.4 wave 2~5 답습)

T1~T11 모두 창의적 풀어쓰기 (rephrasing) 라서 `**원본**` / `**수정 후**` 페어 사용 불가. v2.4 의 wave 2~5 와 같은 케이스 — task 본문에 `**원본**` 블록 자체 박지 X. plan_byte_check 자연 skip (`**원본**` 블록 0건 → 검증 대상 0).

T12 (manifest bump) 만 mechanical 변경이라 byte-copy 룰 적용 가능. multi-step 으로 6 manifest 한 task 묶음.

## §2 위험 코드 지점 (R-1~R-6 매핑)

### R-1: 영어 식별자 실수로 변경

영어 식별자 자체는 변경 X 가 절대 룰. 한국어 매핑이 들어가면 도구 호출 깨짐.

- **mitigation 박힐 위치**: 각 task 끝의 self-review 단계. grep 으로 변경 전후 식별자 count 동일 검증.
- **검증 grep**:
  ```bash
  git diff HEAD~1 HEAD -- skills/<X>/SKILL.md | grep -E "^[-+].*\b(AskUserQuestion|TaskCreate|plan_byte_check|generating-html|auto-brainstorming|verifying-spec|change-history|writing-plans|designing-direction|executing-plans|brainstorming|js-super-sub-driven|change-propagation|risk-annotation|code-pretty|setting-up-worktrees|worktree-merge-back)\b" | grep -v "^[-+]$"
  ```
  expected: 식별자 자체의 추가/삭제 0 (같은 라인 안 한국어 단어 매핑은 OK).

### R-2: 단어 매핑이 맥락에 안 맞는 경우

`override` / `slug` / `manifest` 등은 맥락 따라 다르게 매핑.

- **mitigation**: 일괄 grep 치환 금지. 각 task 안에서 줄별 / 맥락별 매핑.
- **검증**: 작업 끝나고 사람 읽어보기 — auto 모드라 메인이 직접 self-review.

### R-3: skill body 의 LLM prompt 영역과 사용자 노출 영역 경계 모호

Process Step prose 가 두 가지 다 가능. 메인이 보고 판단.

- **mitigation**: 보수적 — 형식 변경 최소, 단어 매핑 적극.

### R-4: og-* skill 실수 변경

upstream mirror. 변경 절대 X.

- **mitigation**: og-* 3 skill / 3 command 작업 대상 list 에서 명시 제외. 각 task 의 Files 목록에서 og-* X.
- **검증 grep**:
  ```bash
  git diff HEAD~1 HEAD -- skills/og-*/SKILL.md commands/og-*.md
  ```
  expected: 빈 결과.

### R-5: 23 파일 동시 변경의 리뷰 부담

- **mitigation**: wave 단위 commit. Wave 1 끝 commit / Wave 2 끝 commit / Wave 3 끝 commit / Wave 4 끝 commit.

### R-6: 본 작업의 메인 turn 답습

본 작업 중 메인이 답변에서 또 형식적 표 / 영어 단어 다발 / 메모 마커 쓰면 사용자가 catch.

- **mitigation**: 본 작업 시작부터 메인 답변도 v2.4.2 룰 답습. 자기 답변 모니터링.

## §3 Task 자세 (T1~T12)

### T1 — brainstorming skill 본문 풀어쓰기

**Files**: `skills/brainstorming/SKILL.md`

**Model**: sonnet

**TDD steps**:

1. `skills/brainstorming/SKILL.md` 본문 전체 Read
2. 사용자 노출 영역 catch — `ℹ️` 안내 / Process Step prose / Anti-Patterns 표 한글 본문 / Why-How 한글 / 사용자 인용 / 회귀 catch 메모
3. 줄별 / 맥락별 풀어쓰기 — 영어 단어 매핑 (`dispatch` 등 v2.4 A-3 답습) + 형식 정리 (표 / 메모 마커 / 메뉴식 옵션 → 줄글)
4. 영어 식별자 / 함수명 / 파일 경로 / 도구명 / git 표준 용어는 그대로
5. Edit tool 로 변경 적용
6. self-review grep — 식별자 count 동일 검증 (R-1) + og-* X 확인

**Acceptance**:
- 사용자 노출 영역 한글 본문이 자연스러운 줄글
- 영어 식별자 count 변경 0
- 표 / 메모 마커 (`→`, `✅`, `❌`) 본문 안 잔존 = 코드 블록 / 표 안만 (사용자 노출 prose 에서는 0)

### T2 — designing-direction skill 본문 풀어쓰기

**Files**: `skills/designing-direction/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습. designing-direction 의 adaptive 7-topic 안내 본문 / Step 1~7 prose / Adaptive Topics 섹션 / Anti-Patterns 표 모두 정리.

**Acceptance**: T1 동.

### T3 — writing-plans skill 본문 풀어쓰기

**Files**: `skills/writing-plans/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습. writing-plans 의 Task Model Hint 섹션 / Same-file 묶음 룰 본문 / Self-Review 본문 등 정리. `**Model**` / `**Files**` 같은 표 라벨은 영어 식별자라 그대로.

**Acceptance**: T1 동.

### T4 — executing-plans skill 본문 풀어쓰기

**Files**: `skills/executing-plans/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습. executing-plans 의 git-fast vs memory-fallback 안내 / per-task 흐름 prose / Critical-Non-critical 판정 룰 본문 / AskUserQuestion 강제 룰 본문 정리.

**Acceptance**: T1 동.

### T5 — auto-brainstorming skill 본문 풀어쓰기

**Files**: `skills/auto-brainstorming/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습. auto-brainstorming 의 Step 1~6 prose / transition notice 문구 / Anti-Patterns 표 한글 본문 정리.

**Acceptance**: T1 동.

### T6 — auto-designing-direction skill 본문 풀어쓰기

**Files**: `skills/auto-designing-direction/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습.

**Acceptance**: T1 동.

### T7 — auto-writing-plans skill 본문 풀어쓰기

**Files**: `skills/auto-writing-plans/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습.

**Acceptance**: T1 동.

### T8 — auto-executing-plans skill 본문 풀어쓰기

**Files**: `skills/auto-executing-plans/SKILL.md`

**Model**: sonnet

**TDD steps**: T1 답습.

**Acceptance**: T1 동.

### T9 — auto-* 4 commands 풀어쓰기

**Files**: `commands/auto-brainstorm.md` / `commands/auto-design.md` / `commands/auto-execute-plan.md` / `commands/auto-write-plan.md`

**Model**: sonnet

**TDD steps**:

1. 4 파일 각각 Read
2. description / 사용법 / 옵션 설명 prose 한글 본문 catch
3. 줄별 풀어쓰기 + 형식 정리
4. 4 파일 각자 Edit
5. self-review grep — og-* X 확인

**Acceptance**: 4 파일 모두 한글 본문 자연스러운 줄글 + 영어 식별자 보존.

### T10 — PRD 4 commands 풀어쓰기

**Files**: `commands/brainstorm.md` / `commands/design.md` / `commands/execute-plan.md` / `commands/write-plan.md`

**Model**: sonnet

**TDD steps**: T9 답습.

**Acceptance**: T9 동.

### T11 — 기타 7 commands 풀어쓰기

**Files**: `commands/api-test.md` / `commands/audit-risk.md` / `commands/fast-tasks.md` / `commands/pretty-md.md` / `commands/sync-html.md` / `commands/worktree.md` / `commands/worktree-merge-back.md`

**Model**: sonnet

**TDD steps**: T9 답습. 7 파일 각자 정리. sync-html 의 `--check` 옵션 섹션 (v2.4.0 Wave 4 추가) 도 같이 톤 정리.

**Acceptance**: T9 동.

### T12 — 6 manifest bump + 백로그 이관 + CLAUDE.md 메타 노트 확인

**Files**:
- `gemini-extension.json`
- `package.json`
- `.cursor-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `.claude-plugin/plugin.json`
- `.codex-plugin/plugin.json`
- `CLAUDE.md` (이미 적용된 메타 노트 잔존 확인)
- `docs/features/2026-05-23-v2.4.2-commands-md-tone-polish/` (commit log 답습)

**Model**: haiku

**TDD steps**:

1. 6 manifest version 2.4.1 → 2.4.2 multi-step Edit (byte-copy `**원본**` / `**수정 후**` 페어)
2. CLAUDE.md 의 "## ⚠️ 본 CLAUDE.md 의 적용 범위" 섹션 잔존 확인 (Wave 0 적용 답습)
3. self-review grep — 모든 manifest version 2.4.2 일치 확인

**Acceptance**:
- 6 manifest 모두 `"version": "2.4.2"`
- CLAUDE.md 메타 노트 잔존
- 백로그 파일 (있다면) shipped 이관 — 본 v2.4.2 는 백로그 파일 없음 (auto-brainstorm 직진), 따라서 이관 대상 없음

## §4 검증 전략

### 4.1 Wave 끝 검증

각 wave commit 직후:

```bash
# 영어 식별자 변경 0 검증
git diff HEAD~1 HEAD -- skills/*/SKILL.md commands/*.md | grep -E "^[-+].*\b(AskUserQuestion|TaskCreate|plan_byte_check|generating-html|auto-brainstorming|verifying-spec|change-history|writing-plans|designing-direction|executing-plans|brainstorming|js-super-sub-driven|change-propagation|risk-annotation|code-pretty|setting-up-worktrees|worktree-merge-back)\b" | grep -v "^[-+]$"
# expected: 식별자 자체의 추가/삭제 0

# og-* 변경 0
git diff HEAD~1 HEAD -- skills/og-*/SKILL.md commands/og-*.md
# expected: 빈 결과
```

### 4.2 본 작업 끝 검증

```bash
# 영어 단어 잔존 (v2.4 A-3 매핑 답습)
grep -nE "\b(dispatch|fire-and-forget|byte-copy|wave-parallel|boilerplate)\b" skills/{brainstorming,designing-direction,writing-plans,executing-plans,auto-*}/SKILL.md commands/{auto-*,brainstorm,design,execute-plan,write-plan,api-test,audit-risk,fast-tasks,pretty-md,sync-html,worktree,worktree-merge-back}.md
# expected: Anti-Patterns 표의 영어 식별자 / 코드 패턴만. 사용자 노출 prose 에서는 0.

# 메모 마커 (→ ✅ ❌) 사용자 노출 영역 잔존
grep -nE "(^|[^\\\\])(→|✅|❌)" skills/{brainstorming,designing-direction,writing-plans,executing-plans,auto-*}/SKILL.md commands/{auto-*,brainstorm,design,execute-plan,write-plan,api-test,audit-risk,fast-tasks,pretty-md,sync-html,worktree,worktree-merge-back}.md
# expected: 코드 블록 / 표 / 진행 노트 1줄 안에서만 (사용자 노출 prose 에서는 0)
```

### 4.3 pytest 답습

`scripts/tests/` 의 61 PASS 답습. 본 작업이 scripts 영향 X 라 회귀 가능성 거의 0 이지만 안전 위해 push 전 실행.

```bash
source .venv/bin/activate && pytest scripts/tests/ -v
# expected: 61 passed
```

### 4.4 dogfood 검증

v2.4.2 push 후 다음 외부 피처 흐름에서 메인의 답변 / TaskCreate 명칭 / 진행 보고 / AskUserQuestion 본문 등이 v2.4.2 톤 답습하는지 사용자 catch.

## §5 Non-goals

- 회귀 catch grep 자동화 (v2.4.3 reserved)
- 보조 skill (change-history / risk-annotation / verifying-spec / generating-html / code-pretty / setting-up-worktrees / finishing-a-development-branch / api-auto-testing) 본문 정리 (v2.4.3 이후 후속 후보)
- og-* / audit-report-prompt / scripts/ 영향
- CLAUDE.md 의 v2.4 A 톤 룰 자체 변경 (예외: contributor 안전망 메타 노트 1 섹션 추가 — Wave 0 이미 적용)
- README.md 본문 톤 보정 (v2.4 Wave 7 의 version badge 만 했고, 본문 톤 보정은 v2.4.3 이후 후속 후보)

## 변경이력

### [2026-05-23] [코드-수정] End-of-Run batch
- **id**: CH-20260523-004
- **이유**: v2.4.2 의 Wave 0 ~ Wave 4 코드 변경 batch 정리. v2.4.0 의 한국어 친화 톤 룰이 CLAUDE.md 에만 박혀서 효과 미흡했던 catch + 사용자 catch 두 가지 ("단어 자체가 어렵다" + "설명 방식 자체가 너무 고지식") 답습.
- **무엇이**:
  - Wave 0 (CLAUDE.md): "⚠️ 본 CLAUDE.md 의 적용 범위" contributor 안전망 메타 노트 1 섹션 추가 (미래 세션이 사용자 영향 룰을 CLAUDE.md 에 박는 회귀 catch)
  - Wave 1 (PRD 4 skill, 8 군데): brainstorming / designing-direction / writing-plans / executing-plans 의 산출물 frontmatter 노트 한국어화 + 영어 한 줄 한국어 + 괄호 메모 제거 + Announce 문구 한국어 + verify-gate 미설치 안내 한국어 + Rationale 한국어 추가
  - Wave 2 (auto-* 4 skill, 3 군데): auto-brainstorming Announce 문구 한국어 + auto-designing-direction ℹ️ 활성 토픽 안내 풀어쓰기 + auto-executing-plans ℹ️ 완료 메시지 풀어쓰기 (auto-writing-plans 는 자연스러움 OK 라 변경 0)
  - Wave 3 (commands 14 파일, og-* 와 audit-report-prompt 와 worktree-merge-back 제외): description / 본문 prose 한국어 친화 풀어쓰기. 영어 단어 (wave-parallel / subagent / Gate #N / fire-and-forget / dispatch / chain / failure isolation / sync) 한국어 매핑 적극 적용. execute-plan 의 "Plan has N tasks" 사용자 prose 도 한국어로 풀어쓰기.
  - Wave 4 (6 manifest): 2.4.0/2.4.1 → 2.4.2 통일 (v2.4.1 patch 가 일부만 bump 했던 catch 정정)
- **영향범위**: 23 파일 변경 (skill 8 + commands 14 + manifest 6 + CLAUDE.md 메타 노트 1 섹션). og-* / scripts / 보조 skill (change-history / risk-annotation / verifying-spec / generating-html / code-pretty / setting-up-worktrees / finishing-a-development-branch / api-auto-testing) / 알람 시스템 / DB / API 영향 0. v2.4.x 룰 모두 보존. pytest 61 PASS 답습.
- **검증 결과**:
  - R-1 영어 식별자 변경: 0 (식별자 자체는 변경 전/후 모두 보존, 같은 라인 안 한국어 단어 매핑만 진행)
  - R-4 og-* 변경: 0 (`git diff -- skills/og-*/SKILL.md commands/og-*.md` 빈 결과)
  - pytest: 61 passed
  - 본 작업의 한계 (SKILL.md 의 사용자 노출 영역이 좁음) 답습 — 변경 8 + 3 + 14 + 6 = 31 군데. 후속 dogfood 사이클로 추가 catch 누적 필요.

### [2026-05-23] [구현계획서-수정]
- **id**: CH-20260523-003
- **이유**: v2.4.2 의 구현 계획 단계. requirements.md (CH-001) + tech-design.md (CH-002) 의 결정을 12 task 4 wave 로 분해.
- **무엇이**: 신규 `v2.4.2-commands-md-tone-polish-implementation-plan.md` — §1 12 task 4 wave 분해 (Wave 1 PRD 4 skill / Wave 2 auto-* 4 skill / Wave 3 commands 15 → 3 task / Wave 4 6 manifest + 메타 노트 확인) + §2 R-1~R-6 mitigation 매핑 + §3 task 자세 (T1~T12) + §4 검증 전략 4 단계 (wave 끝 grep / 본 작업 끝 grep / pytest / dogfood) + §5 Non-goals.
- **영향범위**: 23 파일 (skill 8 + commands 15) + CLAUDE.md 메타 노트 1 섹션 (Wave 0 이미 적용) + 6 manifest bump 2.4.1 → 2.4.2. og-* / scripts / 보조 skill / 알람 시스템 / DB / API 영향 0. v2.4.x 룰 / 알람 시스템 / scripts 보존.
- **verifying-spec 결과**:
  - A1 consistency (requirements ↔ impl-plan): ✅ D1~D6 모두 §1~§5 schema 로 답습
  - A2 consistency (tech-design ↔ impl-plan): ✅ §1 영향 매트릭스 + §5 D-T1~D-T5 + §6 R-1~R-6 + §7 검증 전략 모두 답습
  - C1 code impact: ⚠️ 23 파일 광범위 — Wave 분할 4 단계로 대응, DAG dependency 0 (모든 task 독립)
  - C2 side effect: ⚠️ R-1 영어 식별자 실수 변경 위험 + R-4 og-* 실수 변경 위험 — 각 task 끝 grep 검증 강제
