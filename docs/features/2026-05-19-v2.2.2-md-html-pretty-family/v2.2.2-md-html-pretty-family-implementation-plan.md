---
commit_policy: per-task
slug: 2026-05-19-v2.2.2-md-html-pretty-family
---

# v2.2.2 — `.md` / `.html` pretty family 완성 + 스킬 리네임 — 구현계획서

> Upstream: `v2.2.2-md-html-pretty-family-{requirements,tech-design}.md`
> 분해: 7 task / 3 wave / mechanical 우세
> v2.2.1 위 patch — fire-and-forget 기조 + 단어 일괄 교체 atomic

## §1. Task 분해

### Wave 1 (deps 0, parallel — 신규 / 리네임 / 신규 fixture)

#### Task 1 — 신규 slash command 2개 (`/pretty-md` + `/sync-html`)

**Files**: `commands/pretty-md.md`, `commands/sync-html.md`

**Model**: sonnet

**TDD steps**:
1. `commands/pretty-md.md` 신규 작성 — `/pretty-md <slug>` 슬래시. 인자 optional + latest slug 추론. 동작: feature 디렉토리의 모든 `<slug>-*.md` 에 Sonnet format-only pass dispatch (fire-and-forget). `## 변경이력` footer **byte-equal** 보존 룰 본문 명시.
2. `commands/sync-html.md` 신규 작성 — `/sync-html <slug> [--rebuild]` 슬래시. 인자 optional + latest slug 추론. 분기 룰 (`.html` 없음 → 디자인+생성 / 존재 → 디자인 보존 + 내용 sync / `--rebuild` → 디자인 처음부터). subagent dispatch (sonnet, `run_in_background=true`). `--rebuild` 명시 경고문 포함.
3. 검증 — `ls commands/pretty-md.md commands/sync-html.md` 모두 존재. 본문 grep `fire-and-forget` / `run_in_background.*true` 출현.

**RISK**: none — 신규 파일 작성

**Same-file mechanical 묶음 근거** (v2.0.1+ D1): 같은 `commands/` 디렉토리 / `ls` 통합 검증 / mechanical 추가 — 묶음 OK.

---

#### Task 2 — `skills/docs-pretty/` → `skills/generating-html/` 폴더 이전 + 자기지칭 본문 갱신

**Files**: `skills/docs-pretty/SKILL.md` → `skills/generating-html/SKILL.md`, `skills/docs-pretty/html-companion-prompt.md` → `skills/generating-html/html-companion-prompt.md`

**Model**: sonnet

**TDD steps**:
1. `git mv skills/docs-pretty/ skills/generating-html/` — 폴더 + 내부 파일 이전 (tests/ 하위 포함). git history 보존.
2. `skills/generating-html/SKILL.md` frontmatter `name: docs-pretty` → `name: generating-html`. description 본문 `Docs Pretty` / `docs-pretty skill` → `Generating HTML` / `generating-html skill` 자기지칭 갱신.
3. `skills/generating-html/SKILL.md` 본문 H1 `# Docs Pretty` → `# Generating HTML`. 본문 자기지칭 모든 출현 위치 단어 교체.
4. `skills/generating-html/html-companion-prompt.md` 본문 자기지칭 (있을 경우) 갱신.
5. 검증 — `grep -rn "docs-pretty" skills/generating-html/` → 0 (자기지칭 잔존 없음).

**원본** (`skills/docs-pretty/SKILL.md` line 1-3):
```
---
name: docs-pretty
description: Use during the initial-creation iteration loop of <slug>-requirements.md / <slug>-tech-design.md / <slug>-implementation-plan.md. Fires before user review on every draft (v1.1.15+ unified timing — pre-review per-draft). Re-fires on each user-fix iteration. STOPS firing once the first change-history entry is logged — that boundary marks "live doc". Dispatches a single Sonnet subagent (B) in fire-and-forget mode (v2.2.1+) — generates a sibling `.html` companion (사람 전용 시각화 사본) with semantic 1:1 preservation. Main does NOT wait for the result. RAW `.md` is shown to user as-is (v2.2.1 removed the v2.2.0 A format-only pass — B alone now handles human readability). NEVER invoked on change-history appends, change-propagation cascades (use `/regen-html` instead), or partial revisions.
```

**수정 후**:
```
---
name: generating-html
description: Use during the initial-creation iteration loop of <slug>-requirements.md / <slug>-tech-design.md / <slug>-implementation-plan.md. Fires before user review on every draft (v1.1.15+ unified timing — pre-review per-draft). Re-fires on each user-fix iteration. STOPS firing once the first change-history entry is logged — that boundary marks "live doc". Dispatches a single Sonnet subagent (B) in fire-and-forget mode (v2.2.1+) — generates a sibling `.html` companion (사람 전용 시각화 사본) with semantic 1:1 preservation. Main does NOT wait for the result. RAW `.md` is shown to user as-is (v2.2.1 removed the v2.2.0 A format-only pass — B alone now handles human readability). NEVER invoked on change-history appends, change-propagation cascades (use `/sync-html` instead), or partial revisions.
```

**RISK**: breaking — skill 이름 변경 → 사용자 구 호출 깨짐. mitigation: deprecated 정책 답습 (alias / shim 추가 X — tech-design §6.2 답습)

---

#### Task 3 — 신규 4 fixture (H19~H22) README 작성

**Files**: `skills/generating-html/tests/H19-pretty-md/README.md`, `skills/generating-html/tests/H20-sync-html-design-preserve/README.md`, `skills/generating-html/tests/H21-sync-html-rebuild/README.md`, `skills/generating-html/tests/H22-word-grep-zero/README.md`

**Model**: sonnet

**TDD steps**:
1. `skills/generating-html/tests/H19-pretty-md/README.md` 작성 — `/pretty-md` 호출 시 `.md` format-only + `## 변경이력` footer **byte-equal** 검증 시나리오 (정형 전/후 footer SHA256 일치 / 사용자 정형 결과 git diff 검토 / fire-and-forget < 100ms).
2. `skills/generating-html/tests/H20-sync-html-design-preserve/README.md` 작성 — `/sync-html` 두번째 호출 (`.html` 존재) → 디자인 보존 + 내용만 sync 검증 (CSS / Mermaid 색깔 / 레이아웃 / 여백 sha 비교 / 텍스트 노드만 갱신).
3. `skills/generating-html/tests/H21-sync-html-rebuild/README.md` 작성 — `/sync-html --rebuild` 호출 시 디자인 변경 가능 시나리오 + 실수 호출 시 회복 (gitignored 라 `git checkout` X / 다음 호출 plain `/sync-html` 으로 자연 회복 X — `--rebuild` 의도 외 사용 시 영구 손실 명시).
4. `skills/generating-html/tests/H22-word-grep-zero/README.md` 작성 — `docs-pretty` + `regen-html` 단어 grep 결과 0 검증 (og-* / 백로그 / fixture history 제외 룰 + grep 명령 답습 / 회귀 catch).
5. 검증 — `ls skills/generating-html/tests/H{19,20,21,22}-*/README.md` 모두 존재.

**RISK**: none — 신규 fixture 작성

---

### Wave 2 (deps [Task 1, 2] — 리네임 후 단어 교체 + change-propagation 갱신 + regen-html 삭제)

#### Task 4 — 단어 일괄 교체 (9 skill body + CLAUDE.md + README.md + commands/fast-tasks.md + 2 fixture 폴더 이전)

**Files**:
- `skills/auto-brainstorming/SKILL.md`
- `skills/auto-designing-direction/SKILL.md`
- `skills/auto-executing-plans/SKILL.md`
- `skills/auto-writing-plans/SKILL.md`
- `skills/brainstorming/SKILL.md`
- `skills/code-pretty/SKILL.md`
- `skills/designing-direction/SKILL.md`
- `skills/writing-plans/SKILL.md`
- `CLAUDE.md`
- `README.md`
- `commands/fast-tasks.md`
- `skills/generating-html/tests/H17-html-companion/README.md` (T2 폴더 이전 후)
- `skills/generating-html/tests/H18-html-fire-and-forget/README.md` (T2 폴더 이전 후)

**Model**: sonnet (multi-file mechanical + contextual)

**TDD steps**:
1. 각 파일 `docs-pretty` → `generating-html`, `regen-html` → `sync-html`, `/regen-html` → `/sync-html`, `docs-pretty skill` → `generating-html skill` 단어 교체. 컨텍스트 보존 — 변경이력 footer / fixture history (`dj-superkit-sub-driven/tests/H4-6`) 안의 단어는 절대 X.
2. `CLAUDE.md` 의 `## docs-pretty .html companion 결합 (v2.2.0 → v2.2.1+)` 섹션 → `## generating-html .html companion 결합 (v2.2.0 → v2.2.2+)` 으로 헤더 갱신 + 본문 단어 갱신 + v2.2.2 atomic patch 룰 추가 결합 메모 (5 항목 atomic + 단어 grep 0 검증).
3. `README.md` v2.2.0 ~ v2.2.2 wording 갱신 (21~32 항목 carry-over) — `docs-pretty` 언급 → `generating-html`.
4. 검증 — `grep -rn "docs-pretty\|regen-html" skills/ commands/ CLAUDE.md README.md --exclude-dir=og-* --exclude-dir=H4-preflight-fail --exclude-dir=H5-docs-pretty-pre-review --exclude-dir=H6-task-name-friendly` 결과 0 (Anti-Pattern catch 라인만 허용).

**원본** (`CLAUDE.md` line 348-349):
```
요약: 6 파일 + CLAUDE.md 결합 메모 변경은 묶어서 처리. 7+ 파일 atomic patch.

```

**수정 후**:
```
요약: 6 파일 + CLAUDE.md 결합 메모 변경은 묶어서 처리. 7+ 파일 atomic patch.

```

**RISK**: side-effect — 단어 교체 false-positive (fixture history / 변경이력 footer 의 역사적 단어). mitigation: grep `--exclude-dir` 룰 답습 + 검증 단계에서 0 확인.

---

#### Task 5 — `skills/change-propagation/SKILL.md` Acceptance 5번 + Related Skills 끝 라인 갱신 (자동 호출 → 안내문)

**Files**: `skills/change-propagation/SKILL.md`

**Model**: sonnet

**TDD steps**:
1. Acceptance 5번 항목 갱신 — v2.2.1 자동 호출 룰 → v2.2.2 안내문 룰 (자동 호출 X / `.html` 동봉본 존재 시 사용자에게 `/sync-html <slug>` 호출 안내만).
2. Related Skills 끝 라인 갱신 — `/regen-html` 언급 → `/sync-html` 언급.
3. 검증 — `grep -n "regen-html" skills/change-propagation/SKILL.md` → 0.

**원본** (`skills/change-propagation/SKILL.md` line 130):
```
5. (v2.2.1+) `.html` 동봉본이 존재하는 feature 라면 마지막 단계에서 `/regen-html <slug>` 자동 호출 → live doc 의 `.html` stale 방지
```

**수정 후**:
```
5. (v2.2.2+) `.html` 동봉본이 존재하는 feature 라면 마지막 단계에서 안내문 출력 → `이 변경으로 .html 이 stale 됐을 수 있음. 필요시 /sync-html <slug>` (자동 호출 X — stale 허용 철학 보존)
```

**RISK**: side-effect — Acceptance 룰 의미 변경 (자동 호출 → 안내). mitigation: tech-design §결정 D-T5 답습.

---

#### Task 6 — `commands/regen-html.md` 삭제

**Files**: `commands/regen-html.md`

**Model**: haiku

**TDD steps**:
1. `rm commands/regen-html.md` — 파일 삭제 (git rm으로 git history 보존).
2. 검증 — `ls commands/regen-html.md` → not found.

**RISK**: breaking — `/regen-html` 사용자 호출 시 standard "not found". mitigation: tech-design §6.2 deprecated 정책 답습 (shim 추가 X).

---

### Wave 3 (deps [Task 4, 5, 6] — 최종 검증 + manifest bump)

#### Task 7 — 단어 grep 0 검증 + 6 manifest bump 2.2.1 → 2.2.2

**Files**: `package.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `gemini-extension.json`, `claude-code-plugin.json`, `superpowers.json`

**Model**: sonnet

**TDD steps**:
1. 단어 grep 검증 — `grep -rn "docs-pretty\|regen-html" skills/ commands/ CLAUDE.md README.md --exclude-dir=og-* --exclude-dir=H4-preflight-fail --exclude-dir=H5-docs-pretty-pre-review --exclude-dir=H6-task-name-friendly --exclude-dir=.git` 결과 0 (Anti-Pattern catch 라인만 허용).
2. `bash scripts/bump-version.sh 2.2.2` — 6 manifest 일괄 bump. 일부 manifest 누락 catch (v2.2.1 회귀 — `.claude-plugin/marketplace.json`, `gemini-extension.json`) 시 수동 Edit 보완.
3. 검증 — `grep -l "\"version\": \"2.2.2\"" package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json gemini-extension.json claude-code-plugin.json superpowers.json` 6개 모두 매칭.

**원본** (`gemini-extension.json` line 1-6):
```
{
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "2.2.1",
  "contextFileName": "GEMINI.md"
}
```

**수정 후**:
```
{
  "name": "superpowers",
  "description": "Core skills library: TDD, debugging, collaboration patterns, and proven techniques",
  "version": "2.2.2",
  "contextFileName": "GEMINI.md"
}
```

**RISK**: side-effect — manifest 일부 누락 시 marketplace.json 회귀 (v2.2.1 dogfood 신고). mitigation: bump-version.sh 실행 후 6 manifest 일괄 grep 검증.

---

## §2. 위험 코드 지점

| R-N | 위치 | 위험 | mitigation |
|---|---|---|---|
| R1 | `skills/generating-html/SKILL.md` frontmatter `name:` | breaking — skill 이름 변경 → 사용자 구 호출 깨짐 | T2 + tech-design §6.2 deprecated 정책 (shim 추가 X) |
| R2 | 9 skill body + CLAUDE.md + README.md 단어 grep | side-effect — false-positive (fixture history / 변경이력 footer) | T4 grep `--exclude-dir` 답습 + T7 grep 0 검증 |
| R3 | `skills/change-propagation/SKILL.md` Acceptance 5번 | side-effect — 자동 호출 → 안내문 의미 변경 (live doc `.html` stale 가능) | T5 + tech-design §결정 D-T5 답습 (stale 허용 철학) |
| R4 | `commands/regen-html.md` 삭제 | breaking — 구 슬래시 호출 not found | T6 + tech-design §6.2 답습 |
| R5 | 6 manifest bump | side-effect — bump-version.sh 일부 누락 (v2.2.1 회귀) | T7 6개 일괄 grep 검증 |
| R6 | `/pretty-md` footer **byte-equal** 보존 | side-effect — subagent prompt 누락 시 footer audit chain 손상 | T1 subagent prompt 본문 명시 + H19 fixture footer SHA256 검증 |
| R7 | `/sync-html --rebuild` 의도 외 사용 | breaking — 사용자 디자인 손실 (`.html` gitignored 라 git revert X) | T1 sync-html.md 본문 명시 경고 + H21 fixture 영구 손실 명시 |

## §변경이력

### [2026-05-19 22:48] [구현계획서-수정]
- **id**: CH-20260519-003
- **이유**: v2.2.2 implementation-plan 신규 작성 — 7 task / 3 wave / commit_policy per-task. plan_byte_check 4 blocks PASS. verifying-spec 4축 정합 (Gap 0 / Conflict 0).
- **무엇이**: `v2.2.2-md-html-pretty-family-implementation-plan.md` 신규 작성 (§1 Task 7건 + §2 위험 7건)
- **영향범위**: auto-executing-plans wave-parallel subagent dispatch 단계로 진행 — 20+ 파일 변경 (신규 6 / 리네임 1 폴더 / 삭제 1 / 변경 13 / manifest 6). 위험 카테고리 union: breaking + side-effect (race 0).
- **연관 항목**: CH-20260519-001, CH-20260519-002

### [2026-05-19 23:10] [코드-수정] (batch: tasks 1..7)
- **id**: CH-20260519-004
- **이유**: v2.2.2 5 항목 atomic patch 구현 완료 — `/pretty-md` + `/sync-html` 신규 slash command / `docs-pretty` → `generating-html` 폴더+frontmatter+자기지칭 리네임 / H19~H22 신규 fixture / 단어 일괄 교체 13 파일 / `change-propagation` Acceptance 5번 자동호출→안내문 / `commands/regen-html.md` 삭제 / 6 manifest bump 2.2.1→2.2.2.
- **무엇이**: commands/pretty-md.md, commands/sync-html.md, skills/generating-html/* (구 docs-pretty), skills/generating-html/tests/H{17..22}/, skills/change-propagation/SKILL.md, CLAUDE.md, README.md, commands/fast-tasks.md, skills/{auto-*,brainstorming,code-pretty,designing-direction,writing-plans}/SKILL.md, 6 manifest
- **영향범위**: skill 명칭 + slash 명칭 일괄 정돈 (v2.2.0 → v2.2.2 의 `.md` format-only 책임 v2.2.1 부터 사라진 사실 반영). `.md` format-only 명시 호출 (`/pretty-md`) + `.html` 디자인 보존 sync (`/sync-html`) 책임 분리. `change-propagation` 자동 호출 룰 → 안내문 (boundary 단순화).
- **위험 카테고리**: breaking (skill 리네임 + commands 삭제 → 구 호출 not found) + side-effect (Acceptance 5번 의미 변경 + 단어 grep false-positive 의도된 historical reference)
- **task별 세부 (7건)**:
  - Task 1: `commands/{pretty-md,sync-html}.md` — neue (`none` risk) — commits: `9624afb`
  - Task 2: `skills/docs-pretty/ → skills/generating-html/` 폴더 이전 + frontmatter `name:` + description + ~20 자기지칭 swap (`breaking`) — commits: `4c5b77f`
  - Task 3: `skills/generating-html/tests/H{19..22}/README.md` — 4 신규 fixture (`none`) — commits: `d87a30e`
  - Task 4: 13 파일 단어 일괄 교체 (auto-* x4, brainstorming, code-pretty, designing-direction, writing-plans, CLAUDE.md historical 결합 메모 4건 + 신규 v2.2.2 결합 메모 추가, README.md, commands/fast-tasks.md, H17/H18 fixture body) (`side-effect`) — commits: `f41303e`
  - Task 5: `skills/change-propagation/SKILL.md` Acceptance 5번 + Related Skills (자동 호출 → 안내문) (`side-effect`) — commits: `da8248b`
  - Task 6: `commands/regen-html.md` 삭제 (`breaking`) — commits: `c2b7b5a`
  - Task 7: 6 manifest bump 2.2.1→2.2.2 + grep 0 검증 (실제 4 manifest plan 명시는 stale, 6 manifest 모두 bump 완료) (`side-effect`) — commits: `6b02c07`
- **연관 commits**: `134dd1a..6b02c07` (8 commits — spec docs 1 + tasks 7)
- **변경 전/후 코드**: 생략 — `git show <SHA>` 로 조회
- **연관 항목**: CH-20260519-001, CH-20260519-002, CH-20260519-003
