# v2.2.2 — `.md` / `.html` pretty family 완성 + 스킬 리네임 — 개발방향

> 입력: `v2.2.2-md-html-pretty-family-requirements.md`
> Mode: auto-flow (Socratic) — adaptive 7-topic, AI 자동 design decision (D-T3)

## §1. 외부 의존성 / 인터페이스 (활성)

### 1.1 신규 slash command 2개

| 명령 | 인자 | 핵심 동작 |
|---|---|---|
| `/pretty-md <slug>` | slug optional (latest 추론) | feature 디렉토리의 모든 `<slug>-*.md` 에 Sonnet format-only pass dispatch. `## 변경이력` footer **byte-equal** 보존. fire-and-forget. |
| `/sync-html <slug> [--rebuild]` | slug optional + `--rebuild` 플래그 | `.html` 부재 → 디자인+생성 / 존재 → 디자인 보존 + 내용 sync / `--rebuild` → 디자인 처음부터. fire-and-forget. |

### 1.2 신규 skill 1개 (리네임)

- `skills/docs-pretty/` → `skills/generating-html/` (폴더 + frontmatter `name:` + 자기지칭 본문 전부 교체)
- 책임: `.html` 생성만 (v2.2.1 답습)
- 발동: spec 작성 직후 fire-and-forget (v2.2.1 답습) + `/sync-html` 명시 호출

### 1.3 변경 slash command 1개

- `commands/regen-html.md` 삭제 → `commands/sync-html.md` 신규 (이름 + 동작 변경)

### 1.4 변경 skill 1개

- `skills/change-propagation/SKILL.md` 마지막 단계 안내문 한 줄 추가 (`/sync-html` 자동 호출 X — `/regen-html` 자동 호출 룰 v2.2.1+ 제거)

### 1.5 subagent_type

- `general-purpose` (v2.2.1 답습, U2 결정)

### 1.6 deprecated 안내 — 별도 shim 파일 추가 X

- `docs-pretty` 폴더 + `regen-html.md` 삭제 (fallback 없음)
- 사용자가 구 명령 호출 시 Claude Code 가 "skill not found" 표시 — 표준 동작 답습
- 별도 deprecated 안내 메시지 추가 X (alias / shim 추가 시 영구화 위험)
- 사용자가 신규 명령으로 직접 갱신

## §2. 데이터 모델 / 상태 (활성)

### 2.1 source-of-truth 매트릭스

| 자산 | source | derived | sync 책임 |
|---|---|---|---|
| `.md` (3 spec MD) | spec workflow (사람 + 메인 작성) | — | 자동 (auto-flow) |
| `.md` 정형본 | — | `.md` 자체 (in-place overwrite) | **사용자 명시** (`/pretty-md`) |
| `.html` 사이드카 | `.md` (parsing) | 같은 디렉토리, 같은 basename | 자동 (첫 spec 작성) + 사용자 명시 (`/sync-html`) |

### 2.2 `## 변경이력` footer 보존 룰

- `/pretty-md` 가 `.md` 본문 정형 시 `## 변경이력` 섹션 **byte-equal** 보존
- 정형 대상 = `## 변경이력` 헤더 위 본문만
- 검증: 정형 전/후 footer 영역 SHA256 일치 fixture

### 2.3 디자인 보존 vs 재생성

| 호출 | `.html` 존재 | 동작 |
|---|---|---|
| `/sync-html <slug>` | 없음 | 디자인 + 생성 (generating-html 동등 경로) |
| `/sync-html <slug>` | 있음 | 기존 `.html` 을 subagent 입력 → 디자인·레이아웃·색·여백 보존 + 내용만 sync |
| `/sync-html <slug> --rebuild` | 무관 | 디자인 처음부터 다시 |

### 2.4 `.html` ↔ `.md` 양방향 sync 절대 X

- `.md` 가 영구 source
- `.html` 변경은 사용자가 직접 안 함 (편집 시 다음 `/sync-html` 호출 때 overwrite)
- fixture 에 양방향 sync 감지 grep 추가 X (단순 disabled)

## §4. 성능 / 확장성 (활성)

### 4.1 fire-and-forget 답습

- `/pretty-md` / `/sync-html` / `generating-html` skill 자동 발동 모두 `run_in_background=true` (v2.2.1 답습)
- 메인 turn elapsed 차이 목표 < 100ms (백로그 검증 룰 답습)

### 4.2 디바운스 정책

| 명령 | 디바운스 | 이유 |
|---|---|---|
| `generating-html` 자동 발동 (spec 작성 후) | 3초 (v2.2.1 답습) | 연속 fix 시 비용 누적 방지 |
| `/sync-html` 명시 호출 | **X (매번 dispatch)** | opt-in 명시 호출 — 의도된 행위 |
| `/pretty-md` 명시 호출 | **X (매번 dispatch)** | U1 결정 — opt-in 명시 호출 |

### 4.3 비용 분석

| 흐름 | Sonnet subagent 호출 |
|---|---|
| spec 작성 (3 MD) | 3 × `.html` 생성 (각 디바운스 적용) |
| `/pretty-md <slug>` 1회 | 존재하는 `.md` 수 × Sonnet (보통 1~3회) |
| `/sync-html <slug>` 1회 | 1 × Sonnet |
| `/sync-html <slug> --rebuild` 1회 | 1 × Sonnet |

→ 추가 비용 ε. 자동 발동 경로 미증가 (`/pretty-md` 자동 발동 X).

### 4.4 latency 목표 fixture

- spec 작성 직후 `git diff HEAD~1 HEAD` 사이 메인 turn 시간 측정 (v2.2.1 fixture H18 답습)
- 목표 < 100ms (Task tool dispatch 비용만, 결과 대기 X)

## §5. 에러 / 실패 모드 (활성)

### 5.1 단어 grep false-positive

| 위치 | 처리 |
|---|---|
| og-* skill 본문 (`skills/og-*/`) | 제외 (upstream mirror — 절대 손대지 X) |
| 백로그 history (`docs/backlog/`) | 제외 (역사적 명칭 보존) |
| 변경이력 footer (`docs/features/**/*-implementation-plan.md`) | 제외 (CH entry 역사 보존) |
| 본문 영문 식별자 (skill 본문 라인) | **교체 대상** — atomic patch 룰 |
| CLAUDE.md 결합 메모 | **교체 대상** — 단어 갱신 |
| commands/*.md (slash command 본문) | **교체 대상** |

검증 grep:
```bash
grep -rn "docs-pretty\|regen-html" skills/ commands/ CLAUDE.md README.md \
  --exclude-dir=og-* --exclude-dir=archived \
  --exclude-dir=H4-preflight-fail --exclude-dir=H5-docs-pretty-pre-review --exclude-dir=H6-task-name-friendly
# expected: 0 (Anti-Pattern catch 라인만 허용)
# 추가 제외: js-super-sub-driven/tests/H4-6 = fixture history (역사적 명칭)
```

### 5.2 리네임 회귀

| 회귀 시나리오 | catch |
|---|---|
| `skills/docs-pretty/` 폴더 잔존 | `ls skills/docs-pretty/` → 디렉토리 없음 |
| frontmatter `name: docs-pretty` 잔존 | `grep -rn "name: docs-pretty" skills/` → 0 |
| 자기 지칭 (`docs-pretty skill`) 잔존 | §5.1 grep |
| 다른 skill 의 호출 명령 잔존 (`docs-pretty` invoke) | §5.1 grep |
| `commands/regen-html.md` 잔존 | `ls commands/regen-html.md` → not found |

### 5.3 `/pretty-md` 결과 사용자 불만 케이스

- 게이트 없음 (U3 결정 — 그냥 덮어쓰기)
- 사용자가 `.md` 정형 결과 마음에 안 들 시 → `git checkout <md>` 로 revert
- 정형 후 fire-and-forget 이므로 메인 흐름은 차단 X (다음 turn 사용자가 결정)

### 5.4 `/sync-html --rebuild` 의도 외 사용

- 사용자가 plain `/sync-html` 으로 디자인 보존 의도였는데 실수로 `--rebuild` 호출 → 디자인 변경
- 회복: 사용자가 `git checkout <html>` (단, `.html` 은 gitignored 라 git 보존 X)
- mitigation: `commands/sync-html.md` 본문에 `--rebuild` 명시 경고 ("기존 디자인 폐기됩니다")
- v2.2.3 dogfood 후 `--rebuild` 사용 빈도 ↓ 시 제거 후보

### 5.5 `/pretty-md` 의 `## 변경이력` footer 손상

- subagent 가 footer 도 정형 (의역 / 들여쓰기 변경 / 순서 변경) 시 audit chain 깨짐
- mitigation: subagent prompt 에 "footer **byte-equal** 보존" 명시
- 검증: 정형 전/후 footer SHA256 자동 검사 fixture

## §6. 호환성 / 마이그레이션 (활성)

### 6.1 5 항목 atomic patch 룰

한 곳만 누락 시 회귀:

| 누락 | 증상 |
|---|---|
| `/pretty-md` 만 추가 + 리네임 안 함 | `.md` 정형 가능하지만 `.html` 명령 이름 거짓말 잔존 |
| 리네임만 + `/pretty-md` 안 추가 | `.md` 정형 수단 영구 부재 → 백로그 D1 미해결 |
| 단어 교체만 누락 | 사용자 혼란 (구 명령 호출 → not found, 신규 명령 명시 안내 부재) |
| `change-propagation` 힌트만 누락 | live doc 후 `.html` stale 인지 불가 (v2.2.1 자동 호출 룰 제거 후 가시성 누수) |
| `--rebuild` 만 누락 | 디자인 변경 케이스 cover 안 됨 (백로그 명시 위반) |

→ 5 항목 한 release atomic patch.

### 6.2 deprecated 안내 — 최소 표현 (U4 결정)

- 별도 deprecated alias / shim 추가 X
- 사용자가 구 명령 (`docs-pretty` skill / `/regen-html` slash) 호출 시 표준 "not found" 응답
- 신규 명령 학습은 README + CHANGELOG 답습

### 6.3 v2.2.1 → v2.2.2 변화 표

| 항목 | v2.2.1 | v2.2.2 |
|---|---|---|
| `.md` format-only 수단 | 없음 (백로그 D1) | `/pretty-md` |
| `.html` 생성 skill 이름 | `docs-pretty` | `generating-html` |
| `.html` 동기 명령 | `/regen-html` | `/sync-html` (+ `--rebuild`) |
| `change-propagation` 끝 `.html` 동기 | 자동 호출 (Acceptance 5번) | 안내문만 (자동 호출 X) |
| `change-propagation` 의 `.html` stale 가시성 | 보장 (자동) | 사용자 판단 (안내 무시 가능) |
| boundary | live doc 진입 후 `.html` 자동 갱신 X | 동일 |

### 6.4 README + CLAUDE.md 갱신

- README 21~32 항목에 v2.2.2 wording 추가 (HANDOFF 미완 carry-over)
- CLAUDE.md `docs-pretty .html companion 결합` 섹션 → `generating-html .html companion 결합` 으로 갱신 + 단어 일괄 교체

## §7. 비활성 토픽

- **§3 권한 / 보안** — N/A (skill 본문만, 외부 access 없음)
- **§7 모니터링 / 관측성** — N/A (silent log `.js-super/html-regen.log` v2.2.1 답습 — 변경 X)

## §결정 + 대안 비교

### D-T1: 5 항목 한 release (atomic patch) vs 분할

| 대안 | 장 | 단 | 채택 |
|---|---|---|---|
| **A1 — 5 항목 atomic patch** | 단어 grep 일관성 + 회귀 catch 쉬움 + 한 release | 부분 release 어려움 (e.g., 리네임만 보고싶을 때) | ✅ |
| A2 — 분할 (리네임 v2.2.2 / 신규 명령 v2.2.3) | 사용자 변경 surface 작음 | 단어 일관성 일시 깨짐 → 잠재 회귀 카운트 ↑ | X |

이유: 5 항목 모두 "`.md` / `.html` pretty 책임 정합" 한 주제 — 분할 의미 0.

### D-T2: `/sync-html --rebuild` 포함 vs 제거

| 대안 | 장 | 단 | 채택 |
|---|---|---|---|
| **A1 — 포함** | 디자인 실험 / 사용자 디자인 불만 케이스 cover | 의도 외 사용 시 디자인 손실 (5.4) | ✅ |
| A2 — 제거 | 단순 | 디자인 변경 수단 부재 → 사용자가 `.html` 직접 편집 → 양방향 sync 진입 위험 | X |

이유: 디자인 실험 + 사용자 자유도 확보. v2.2.3 dogfood 후 사용 빈도 ↓ 시 제거 옵션.

### D-T3: `/pretty-md` 결과 게이트 vs 그냥 덮어쓰기

| 대안 | 장 | 단 | 채택 |
|---|---|---|---|
| **A1 — 그냥 덮어쓰기 (게이트 X)** | fire-and-forget 기조 + 메인 흐름 비차단 | 사용자 마음에 안 들 시 revert 필요 (git diff + checkout) | ✅ |
| A2 — 사용자 결과 보고 게이트 (preview / approve) | 의도와 맞는지 확인 가능 | 메인 흐름 차단 + v2.2.1 단순화 무화 | X |

이유: fire-and-forget 답습 + revert 비용 낮음 (`git checkout`). 백로그 명시 답습.

### D-T4: deprecated 안내 — shim 추가 vs 없음

| 대안 | 장 | 단 | 채택 |
|---|---|---|---|
| **A1 — shim 없음 (표준 not found)** | 단순 + 잔존 회귀 catch 쉬움 (grep 0) | 사용자 한번은 not found 봄 | ✅ |
| A2 — alias / wrapper shim 추가 (`docs-pretty` → `generating-html` 자동 redirect) | UX 부드러움 | deprecated 영구화 + 단어 grep false-positive + 책임 모호 | X |

이유: 단어 일괄 교체 grep 0 룰 (백로그 명시) 답습 + deprecated 영구화 방지.

### D-T5: change-propagation 끝 — 자동 호출 vs 안내

| 대안 | 장 | 단 | 채택 |
|---|---|---|---|
| **A1 — 안내문만 (자동 호출 X)** | 사용자 stale 허용 철학 + boundary 보존 (live doc 후 `.html` 자동 갱신 X 의 자연 확장) | 사용자가 안내 무시 시 `.html` 영구 stale | ✅ |
| A2 — 자동 호출 (v2.2.1 Acceptance 5번 답습) | `.html` 항상 fresh | live doc boundary 룰 위반 + 비용 ↑ + 사용자 의도와 다를 가능성 | X |

이유: v2.2.2 의 컨셉 (`.html` = 사람 전용 derived view, 사용자 명시 호출 중심) 답습. v2.2.1 의 자동 호출 룰은 release 시점 가설이었고, dogfood 검증 안 됨 → boundary 단순화 우선.

### D-T6: `generating-html` 이름 추가 변경 (Non-goals, v2.2.3 후보)

| 대안 | 장 | 단 | 채택 |
|---|---|---|---|
| **A1 — `generating-html` 채택 (v2.2.2)** | 백로그 명시 답습 + "사람 전용 derived view" 책임 분명 (`generating-...` 패턴) | "사람 전용" 컨셉 명시 X — 후속 사용자가 오해 가능 | ✅ (v2.2.2 안) |
| A2 — `html-companion` / `html-sidecar` 등 | "사이드카" 컨셉 직접 표현 | v2.2.2 안 도입 시 리네임 비용 2회 (docs-pretty → generating-html → html-companion) | X (v2.2.3 후보로 이월) |

이유: v2.2.2 atomic patch 안에 이름 추가 변경 비용 추가 X. dogfood 후 v2.2.3 에서 결정.

## §Non-goals (v2.2.2 안 함, 명시)

requirements 의 Non-goals 답습 + 본 tech-design 안 명시:

| Non-goal | 이유 |
|---|---|
| `/pretty-both` 결합 슬래시 | v2.2.3 후보. v2.2.2 는 두 명령 책임 분리 보고 사용 빈도 dogfood. |
| `.md` format-only pass 자동 발동 부활 | 절대 X — v2.2.1 단순화 무화 위험. `/pretty-md` 는 opt-in 명시 호출만. |
| `.html` 디자인 versioning (롤백) | 안 함 — `.html` 은 derived view. 디자인 변경 = `--rebuild` 1회 호출만. |
| `.html` ↔ `.md` 양방향 sync | 절대 X — `.md` 가 영구 source. §2.4 답습. |
| `generating-html` 이름 추가 변경 | v2.2.3 후보 — §결정 D-T6 답습. |

## §3. 권한 / 보안 (비활성)

N/A — skill 본문 + slash command 만 추가, 외부 access 없음.

## §변경이력

### [2026-05-19 22:42] [개발방향-수정]
- **id**: CH-20260519-002
- **이유**: v2.2.2 tech-design 신규 작성 — adaptive 7-topic (활성 5: §1/§2/§4/§5/§6 / 비활성 2: §3/§7) + 6 결정 (D-T1~D-T6) + verifying-spec 보강 2건 (Non-goals 섹션 신설 + fixture history 제외 grep).
- **무엇이**: `v2.2.2-md-html-pretty-family-tech-design.md` 신규 작성 (§1 외부 인터페이스 / §2 데이터 모델 / §4 성능 / §5 에러 / §6 호환성 / §결정 6건 / §Non-goals 5건)
- **영향범위**: implementation-plan 후속 작성 — 신규 2 파일 (commands/pretty-md.md + sync-html.md) + 리네임 1 폴더 (skills/docs-pretty/ → skills/generating-html/) + 삭제 1 파일 (commands/regen-html.md) + 본문 변경 18 파일 (skill body 9 + root 2 + fixture 5 + commands 2) + 신규 fixture 4 (H19~H22) + 6 manifest bump
- **연관 항목**: CH-20260519-001
