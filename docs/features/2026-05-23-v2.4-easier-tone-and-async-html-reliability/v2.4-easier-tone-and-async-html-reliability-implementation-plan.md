---
slug: v2.4-easier-tone-and-async-html-reliability
commit_policy: per-task
total_tasks: 11
---

# v2.4 — 안내 톤 쉽게 + 비동기 .html 신뢰성 (구현계획)

**연관**: `v2.4-easier-tone-and-async-html-reliability-requirements.md` (CH-001) / `v2.4-easier-tone-and-async-html-reliability-tech-design.md` (CH-002)

## §1 작업 단위 (11 task, 7 wave)

| Wave | Task | 파일 / 모드 | byte-copy |
|---|---|---|---|
| 1 | T1 — CLAUDE.md 글로벌 톤 룰 + A/B 결합 메모 | CLAUDE.md (sonnet) | ✅ append |
| 2 | T2 — 4 PRD 흐름 skill 사용자 노출 안내문 한국어 친화 보정 | 4 skill (sonnet 자율) | ❌ rewriting |
| 3 | T3 — auto-* 4 skill 사용자 노출 안내문 한국어 친화 보정 | 4 skill (sonnet 자율) | ❌ rewriting |
| 4 | T4 — commands/*.md 10+ 파일 사용자 안내문 + sync-html --check 옵션 | commands/* (sonnet 자율 + haiku 옵션 추가) | ❌ rewriting + ✅ 옵션 |
| 5 | T5 — README.md 사용자 안내 섹션 보정 | README.md (sonnet 자율) | ❌ rewriting |
| 6 | T6 — generating-html B-1 + B-3 + B-4 룰 (dispatch verify / silent log / 메인 보고) | generating-html SKILL.md (sonnet append) | ✅ append |
| 6 | T7 — auto-brainstorming Step 4.5 race delay 5초 (B-2) | auto-brainstorming SKILL.md (haiku) | ✅ replace |
| 6 | T8 — auto-designing-direction Step 4.5 race delay 5초 | auto-designing-direction SKILL.md (haiku) | ✅ replace |
| 6 | T9 — auto-writing-plans Step 4.6 race delay 5초 | auto-writing-plans SKILL.md (haiku) | ✅ replace |
| 7 | T10 — 6 manifest bump 2.3.6 → 2.4.0 | 6 manifest (haiku) | ✅ multi-step |
| 7 | T11 — 백로그 shipped/ 이관 | 백로그 mv (haiku) | N/A |

### Wave 간 의존성

- Wave 1~7 모두 독립 (다른 파일 / 다른 단계). DAG dependency 없음.
- 메인 직접 처리 vs subagent dispatch 결정은 실행 단계. 11 task 광범위 → subagent wave-parallel 권장.

### byte-copy 룰 적용 / 면제

- ✅ 적용 (T1 / T6~T10): mechanical text append 또는 short anchor replace. plan_byte_check 통과.
- ❌ 면제 (T2~T5): 창의적 rewriting (사용자 노출 안내문 한국어 친화 보정). byte-copy 블록 자체를 plan 본문에 박지 않음 → plan_byte_check 자연 skip. 결과물 검증은 자동 grep + dogfood spot-check.

## §2 위험 코드 지점 (tech-design §6 R-1~R-12 매핑)

| R-id | file:line | mitigation |
|---|---|---|
| R-1 (A 톤 변경이 skill 동작 영향) | T2~T5 의 모든 skill body | D2 영역 구분 엄격 — 영어 룰 본문 / 식별자 보존, 사용자 노출 안내문만 |
| R-3 (A 톤 룰 LLM 해석 모호) | T1 CLAUDE.md 글로벌 톤 룰 | Before/After 예시 명시 + grep 룰 |
| R-5 (A 검증 자동화 어려움) | T2~T5 결과물 | wave 단위 commit 후 sample spot-check + grep 보조 |
| R-7 (B sync fallback 미도입 → .html 영구 stale) | T6 generating-html B-4 | 메인 보고 룰 + silent log + 사용자 명시 호출 가이드 |
| R-9 (B race 원인이 delay 외) | T6~T9 race delay 적용 | dogfood 5+ 회 sample / log 분석 |
| R-10 (A 광범위 변경이 v2.3.x 회귀) | T2~T5 모든 변경 | og-* 변경 0 / Anti-Pattern grep / v2.3.x 룰 본문 보존 |

R-2, R-4, R-6, R-8, R-11, R-12 — 자연 mitigation (영향 미미 / 후속 후보).

---

## §3 Task 명세

### Task 1 (Wave 1) — CLAUDE.md 글로벌 톤 룰 + A/B 결합 메모

**Files**: `CLAUDE.md`
**Model**: sonnet (한국어 prose)
**Risk**: side-effect (메인 응답 양식 영향)

**원본** (`CLAUDE.md:532-532`):

```
요약: 3 파일 (executing-plans/SKILL.md + dj-superkit-sub-driven/SKILL.md + CLAUDE.md) atomic patch. 5+ 파일 동시 push (3 + 6 manifest + 백로그 mv).
```

**수정 후**: 같은 마지막 라인 + 위에 새 섹션 2개 (한국어 친화 톤 룰 + A/B 결합 메모) append.

신규 본문 (압축):

```
## 한국어 친화 안내 톤 (v2.4+)

dj-superkit 의 사용자 노출 안내문 (메인이 사용자에게 직접 보여주는 모든 문구) 은 다음 룰을 따른다:

### A-1: 짧은 한국어 문장
- 한 문장에 정보 1~2개. 한 문단 4 문장 이하.
- 영어 식별자는 꼭 필요할 때 + 처음 등장 시 한국어 설명 동반.

### A-2: 메모 / 슬래시 / 콜론 다발 금지
- 보고는 완전 문장. `→`, `✅` 마커는 진행 노트 한 줄만.
- 콜론 다발 (`결과: X / 다음: Y`) 보다 풀어쓰기.

### A-3: 한국어-영어 mix 최소
- 사용자 시야 표현은 한국어 우선.
- `fire-and-forget` → `백그라운드 호출`, `dispatch` → `호출`, `byte-copy` → `원본 그대로 보존` 등.

### A-4: 사용자 친화 보고
- 무엇을 했는지 (1~2 문장) + 왜 그랬는지 (필요 시) + 다음 단계 (1 문장).
- 그 외 세부는 자세히 묻기 전 생략.

### A-5: 적용 영역 구분 (중요)

| 영역 | 한국어 친화 톤 적용 |
|---|---|
| skill body 의 식별자, 함수 / 파일 이름 | 영어 그대로 (변경 X) |
| skill body 의 룰 본문 (Why / How / Anti-Patterns 표) | 영어 그대로 (변경 X) — 메인 prompt 가공용 |
| skill body 의 사용자 노출 안내문 (메인 turn 에 그대로 출력) | 한국어 친화 적용 |
| CLAUDE.md 의 글로벌 톤 룰 | 한국어로 추가 (본 섹션) |
| 메인이 사용자에게 보고하는 응답 양식 | 한국어 친화 적용 |
| commands/*.md 사용자 안내문 | 한국어 친화 적용 |
| README.md 사용자 안내 섹션 | 한국어 친화 적용 |

### Before / After 예시

❌ Before (영어-한국어 mix + 메모):
> `✅ fire-and-forget dispatch 완료. byte-copy 룰 보존. → 다음 단계 진행.`

✅ After (한국어 친화):
> 백그라운드 호출이 끝났습니다. 원본 보존 룰 그대로 따랐고, 다음 단계로 넘어갑니다.

### 회귀 catch grep

```bash
grep -c "한국어 친화 안내 톤 (v2.4+)" CLAUDE.md
# expected: ≥ 1
```

## 비동기 .html 신뢰성 룰 (v2.4+)

`generating-html` 백그라운드 호출이 처음 .md 생성 시 가끔 실패하는 회귀 해결. 4 룰:

- **B-1**: dispatch 결과 verify — 호출 직후 id 받았는지 확인 + 시간 경과 후 `.html` 파일 존재 확인
- **B-2**: race condition 해결 — dispatch 후 5초 delay 후에 change-history footer 추가 (background subagent 가 footer 0건 시점에 .md 읽도록 보장)
- **B-3**: silent log monitor — `.dj-superkit/html-regen.log` 에 entry 자동 append, `/sync-html --check` 옵션으로 사용자 조회
- **B-4**: 메인 응답에 dispatch 결과 명시 — transition notice 시점에 완료/실패 보고

### 회귀 catch grep

```bash
grep -c "5초 delay" skills/auto-brainstorming/SKILL.md skills/auto-designing-direction/SKILL.md skills/auto-writing-plans/SKILL.md
# expected: 각 ≥ 1

grep -c "silent log monitor (v2.4+)" skills/generating-html/SKILL.md
# expected: ≥ 1
```

요약: v2.4 메이저 — A 광범위 (10+ skill + 10+ commands + README + CLAUDE.md) + B 4 룰 (generating-html + auto-* 3 + /sync-html). atomic 처리.
```

**TDD Steps**:

1. CLAUDE.md 의 마지막 라인 확인 (실행 시점 `wc -l + tail`)
2. Edit (위 anchor 라인 위에 신규 본문 append)
3. grep 검증 (`한국어 친화 안내 톤 (v2.4+)` + `비동기 .html 신뢰성 룰 (v2.4+)` 각 ≥ 1)

---

### Task 2 (Wave 2) — 4 PRD 흐름 skill 사용자 노출 안내문 보정

**Files**: `skills/brainstorming/SKILL.md`, `skills/designing-direction/SKILL.md`, `skills/writing-plans/SKILL.md`, `skills/executing-plans/SKILL.md`
**Model**: sonnet (창의적 rewriting)
**Risk**: side-effect (R-1 — skill 동작 영향), R-10 (v2.3.x 룰 회귀)
**byte-copy**: ❌ 면제 (창의적 rewriting). plan_byte_check 자연 skip.

**명세**:

각 skill body 안의 "사용자 노출 안내문" 만 한국어 친화 톤으로 보정:

- 영역: 메인이 사용자에게 그대로 출력하는 ℹ️ 안내 / Transition notice / 에러 메시지 / 게이트 prompt / "다음 단계" 안내 등
- 보존: 영어 룰 본문 / Why 섹션 / Anti-Patterns 표 / 식별자 / 함수 이름 / `Skill` `Bash` 등 도구 이름 / digraph identifier / Process Flow 안 영어 노드 이름
- D2 영역 구분 엄격 적용

**TDD Steps**:

1. 각 skill body grep — "사용자 노출 안내문" 영역 식별 (`ℹ️`, "다음 단계", "Transition notice", Process flow 안내, 한국어/영어 mix 라인)
2. 식별된 라인만 Edit (메인 직접 자율 보정 — Before/After 예시 답습)
3. grep 검증 — 영어 식별자 다발 (`fire-and-forget`, `dispatch`, `byte-copy` 등) 가 사용자 노출 영역에서 ↓
4. v2.3.x 룰 본문 grep 보존 확인 (각 skill 의 v2.3.x+ Anti-Patterns 라인 수 변경 X)

---

### Task 3 (Wave 3) — auto-* 4 skill 사용자 노출 안내문 보정

**Files**: `skills/auto-brainstorming/SKILL.md`, `skills/auto-designing-direction/SKILL.md`, `skills/auto-writing-plans/SKILL.md`, `skills/auto-executing-plans/SKILL.md`
**Model**: sonnet
**Risk**: side-effect (R-1, R-10)
**byte-copy**: ❌ 면제

**명세**: T2 와 동일 패턴. auto-* 의 transition notice / Step 별 안내 / "auto-flow 완료" 메시지 등 한국어 친화 보정.

**TDD Steps**: T2 동일.

---

### Task 4 (Wave 4) — commands/*.md 사용자 안내문 + sync-html --check 옵션

**Files**: `commands/*.md` (10+ 파일 — 메인이 실행 시 `ls commands/*.md` 결과 동적 확인)
**Model**: sonnet (rewriting) + haiku (sync-html 옵션 추가)
**Risk**: side-effect (사용자가 slash 명령 호출 시 안내문 변화)
**byte-copy**: ❌ 면제 (rewriting) + ✅ 부분 (sync-html --check 옵션 추가)

**명세**:

1. 각 command body 의 사용자 노출 부분 (한 줄 description / Usage / 다음 단계 안내 등) 한국어 친화 보정
2. `commands/sync-html.md` 에 `--check` 옵션 섹션 추가 — silent log (`.dj-superkit/html-regen.log`) 마지막 10 entry 출력

**TDD Steps**:

1. `ls commands/*.md` 으로 정확 파일 목록 확인
2. 각 파일 사용자 노출 안내문 식별 + 한국어 친화 보정
3. `commands/sync-html.md` 에 `--check` 옵션 섹션 append (mechanical)
4. grep 검증 — 각 commands/*.md 의 영어 식별자 다발 ↓

---

### Task 5 (Wave 5) — README.md 사용자 안내 섹션 보정

**Files**: `README.md`
**Model**: sonnet
**Risk**: side-effect (사용자가 README 읽을 때 안내문 변화)
**byte-copy**: ❌ 면제

**명세**: README.md 의 사용자 안내 섹션 (slash 명령 표 / "시작하기" / "주요 기능" 등) 한국어 친화 보정. 코드 패턴 / 식별자 / 영어 참조 표는 그대로.

**TDD Steps**:

1. README.md 사용자 노출 섹션 식별
2. 식별된 부분 메인 자율 보정
3. grep 검증

---

### Task 6 (Wave 6) — generating-html B-1 + B-3 + B-4 룰

**Files**: `skills/generating-html/SKILL.md`
**Model**: sonnet
**Risk**: side-effect (R-7), R-9
**byte-copy**: ✅ append

**명세**: `generating-html` SKILL.md 끝에 신규 섹션 append:

```markdown
## 비동기 신뢰성 룰 (v2.4+)

### B-1 — dispatch verify

호출 직후 메인이 dispatch id 받았는지 확인 (Agent tool return). 시간 경과 후 (사용자가 transition notice 받기 전) `.html` 파일 존재 확인.

### B-3 — silent log monitor

호출 시 `.dj-superkit/html-regen.log` 에 entry append:

\`\`\`
YYYY-MM-DD HH:MM:SS | DISPATCH | <slug>-<type>.md | agent_id=<id>
YYYY-MM-DD HH:MM:SS | SUCCESS  | <slug>-<type>.html | <bytes> bytes
\`\`\`

`/sync-html --check` 으로 마지막 N entry (default 10) 조회.

### B-4 — 메인 응답 dispatch 결과 명시

호출자 측 (auto-* 3 skill / /sync-html / /audit-risk) 의 transition notice 시점에 명시:
- 완료 시: "백그라운드 호출 완료 (N KB)"
- 실패 시: "실패 — `/sync-html` 으로 사용자가 직접 재시도 필요"

자동 retry 미도입 — 사용자 명시 호출만 (v2.4.x 후속 후보).
```

**원본** (`skills/generating-html/SKILL.md:N-N`): 마지막 라인 1개 (실행 시점 확인)

**수정 후**: 같은 마지막 라인 + 위에 위 본문 append.

**TDD Steps**:

1. SKILL.md 마지막 라인 확인
2. Edit append
3. grep 검증 — `silent log monitor (v2.4+)` ≥ 1

---

### Task 7 (Wave 6) — auto-brainstorming Step 4.5 race delay 5초

**Files**: `skills/auto-brainstorming/SKILL.md`
**Model**: haiku (단순 한 줄 추가)
**Risk**: side-effect (R-9)
**byte-copy**: ✅ replace

**명세**: Step 4.5 본문에 dispatch 후 "5초 delay 후 Step 5 진행" 한 줄 추가.

**원본** (예상 — 실행 시 정확 라인 확인):

```
**왜 v2.3.2+ 도입**: v1.1.17 PRD D9 amend ("auto-flow 는 review 없으므로 prettify 의미 없음") 는 잘못된 가정. transition notice 시점에 사용자가 `.html` 검토 가능 (Type "stop" 으로 abort). v2.3.1 dogfood 에서 사용자 명시 catch — auto-flow 에서도 `.html` 동봉본 필요.
```

**수정 후**: 위 라인 + 그 뒤에 한 줄 추가:

```
**v2.4+ race delay**: dispatch 후 **5초 delay** 후에 Step 5 (change-history) 진행. background subagent 가 .md 의 footer 0건 시점에 읽도록 보장 (race condition 해결).
```

**TDD Steps**:

1. Step 4.5 영역 정확 라인 확인
2. Edit append
3. grep 검증 — `5초 delay` ≥ 1

---

### Task 8 (Wave 6) — auto-designing-direction Step 4.5 race delay

T7 mirror. `skills/auto-designing-direction/SKILL.md` 의 Step 4.5 본문에 같은 한 줄 추가.

**byte-copy**: ✅ replace (T7 패턴 답습)

---

### Task 9 (Wave 6) — auto-writing-plans Step 4.6 race delay

T7 mirror. `skills/auto-writing-plans/SKILL.md` 의 Step 4.6 본문에 같은 한 줄 추가.

**byte-copy**: ✅ replace

---

### Task 10 (Wave 7) — 6 manifest version bump 2.3.6 → 2.4.0

**Files** (6):

- `gemini-extension.json`
- `package.json`
- `.cursor-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `.claude-plugin/plugin.json`
- `.codex-plugin/plugin.json`

**Model**: haiku
**Risk**: none
**byte-copy**: ✅ multi-step

각 파일 version 라인 `"2.3.6"` → `"2.4.0"`.

**원본** / **수정 후** byte-copy 블록 6쌍 (T1~T6 의 v2.3.5 답습 패턴).

**TDD Steps**:

1. 6 Edit (각 manifest 의 version 라인)
2. grep 검증 — `"2.3.6"` 잔존 0 / `"2.4.0"` 각 1

---

### Task 11 (Wave 7) — 백로그 shipped/ 이관

**Files**: `docs/backlog/v2.4-easier-tone-and-async-html-reliability.md` → `docs/backlog/shipped/`

**Model**: haiku
**Risk**: none
**byte-copy**: N/A

**TDD Steps**:

1. `git mv` (이미 tracked 라면) 또는 `mv` (untracked)
2. 검증 — 원위치 부재 + shipped/ 존재

---

## §4 commit 정책

`commit_policy: per-task`. 11 task → 11 commit + [log] consolidator. 또는 wave 단위 묶음 (v2.3.x 답습) — 6 commit (spec + Wave 1~7 batch).

**선택**: wave 단위 묶음 권장 — 11 commit 은 history pollution. 7 commit (wave 1 / 2 / 3 / 4 / 5 / 6 묶음 / 7 묶음) + [log] = 8 commit.

## §5 Acceptance

- T1~T11 모든 task 완료
- §6.1 grep 검증 모두 통과 (tech-design)
- 영향 0 보장: og-* / scripts/* / 알람 시스템 / v2.3.x 룰 본문 모두 그대로
- pytest 61 PASS
- 사용자 spot-check (wave 끝마다 sample 1~2 비교)

## 변경이력

### [2026-05-23 14:15] [구현계획서-수정]
- **id**: CH-20260523-003
- **이유**: v2.4 메이저 — 11 task / 7 wave 분해. byte-copy 룰 부분 적용 (T1 / T6~T10 — append/replace mechanical) + 부분 면제 (T2~T5 — 창의적 rewriting, plan_byte_check 자연 skip). commit 정책: wave 단위 묶음 (8 commit 권장 — spec + Wave 1~7 + [log]).
- **무엇이**: 신규 `v2.4-easier-tone-and-async-html-reliability-implementation-plan.md` — frontmatter (commit_policy per-task, total_tasks 11) + §1 작업 단위 표 (11 task / 7 wave / byte-copy 분류) + §2 R-1~R-12 ↔ task 매핑 + §3 T1~T11 명세 (T1 / T6~T10 byte-copy 블록 + T2~T5 rewriting 명세만) + §4 commit 정책 + §5 Acceptance.
- **영향범위**: 후속 산출물 (실행 단계). 코드 변경 광범위 (CLAUDE.md + 4 PRD skill + 4 auto-* skill + commands/*.md 10+ + README.md + generating-html + 6 manifest + 백로그 mv). plan_byte_check 1 block PASS (T1 anchor 라인 fix 후) + T6~T10 의 line range 부재 블록은 helper 자연 skip. og-* / scripts / 알람 시스템 영향 0.
- **연관 항목**: CH-20260523-001, CH-20260523-002
